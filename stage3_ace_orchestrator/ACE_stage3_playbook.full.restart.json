{
  "bullets": {
    "conflict_and_abstain-00011": {
      "id": "conflict_and_abstain-00011",
      "section": "conflict_and_abstain",
      "content": "When any per-profile pre-call or runtime routing contract check fails (including missing/altered routing_summary in the outgoing payload; payload.candidate_experts != profile.candidate_experts; intended expert_name != routing_summary.chosen_primary_expert_name; first-called != routing_summary.chosen_primary_expert_name; any invoked expert not in profile.candidate_experts; or ZERO experts invoked when candidate_experts non-empty) the orchestrator MUST treat this as a FATAL ROUTING VIOLATION for that profile and immediately perform an atomic replacement of that profile's output with the standardized per-profile abstain JSON EXACTLY. The abstain JSON fields MUST be set as follows: final_label = \"\"; alternative_labels = the ordered profile.candidate_labels; predicted_tissue = \"\"; supporting_genes = the computed pre-call per-profile intersections if available else []; negative_markers = []; expert_models_used = []; confidence = \"low\"; abstain = true. The abstain.rationale MUST be a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (e.g., \"payload.routing_summary mismatch; attempted call to non-candidate expert 'X'\"). Before returning the overall orchestrated response atomically verify that the affected profile's output equals this exact standardized abstain JSON; if any field differs, replace it with the exact standardized abstain JSON. Do NOT accept, synthesize from, reference, or surface any returned expert outputs for that profile, stop further expert calls for that profile, and do not mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.809901+00:00",
      "updated_at": "2026-06-01T21:54:33.809930+00:00"
    },
    "expert_output_synthesis-00001": {
      "id": "expert_output_synthesis-00001",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-NEW-PRESERVE-VERBATIM: Atomic pre-call routing_summary + verbatim-first-expert-preservation — For every profile (only over profile.candidate_experts) the orchestrator MUST atomically: (A) compute a routing_summary and include that routing_summary VERBATIM in the single-profile outgoing payload before any expert call (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). (B) Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call. (C) Upon the first invoked expert return verify atomically: returned.expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; final.predicted_tissue MUST equal returned.canonical_tissue exactly (verbatim; no normalization or paraphrase); final.supporting_genes MUST equal intersection(returned.supporting_genes, supplied_ordered_genes); final.negative_markers MUST equal intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); and final.expert_models_used MUST exactly equal the runtime-ordered invoked experts for that profile. (D) If any of these equalities/checks fail (including missing routing_summary pre-call, predicted_tissue not equal to returned.canonical_tissue verbatim, supporting_genes/negative_markers not equal to the computed intersections, or expert_models_used mismatch), TREAT THIS AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the specific equality failure (e.g., \"predicted_tissue != first_invoked_expert.canonical_tissue\" or \"missing routing_summary in payload\"). Do not accept, synthesize from, or surface any returned expert outputs for that profile when this violation occurs.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.809948+00:00",
      "updated_at": "2026-06-01T21:54:33.809953+00:00"
    },
    "tool_input_restructuring-00002": {
      "id": "tool_input_restructuring-00002",
      "section": "tool_input_restructuring",
      "content": "Strengthened atomic pre-call and first-call enforcement — Extend tool_input_restructuring-00002 to require that the computed routing_summary (per-candidate intersection_count and earliest_index plus chosen_primary_expert_name) be included verbatim in the per-profile tool payload and that the chosen_primary_expert_name MUST be the first expert actually invoked for that profile. Atomically verify before any call: (1) profile.candidate_experts non-empty; (2) intended expert_name is in profile.candidate_experts; (3) routing_summary present and routing_summary.chosen_primary_expert_name exactly equals the intended expert_name; and (4) the orchestrator will invoke that chosen_primary_expert_name as the first/only initial call. After the first expert/tool returns, immediately validate that the returned expert_name equals routing_summary.chosen_primary_expert_name and is a member of that profile's candidate_experts. If any check fails (missing/incorrect routing_summary, intended expert not a candidate, first-called expert != chosen_primary_expert_name, or any attempted/non-logged call to a non-candidate expert), do NOT accept or synthesize from any returned outputs for that profile and emit the standardized abstain output for that profile exactly per conflict_and_abstain templates (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue=\"\", supporting_genes = any computed pre-call intersections or [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation and offending expert. Do not attempt further expert calls for that profile. Also require that final expert_models_used in the synthesized final_answer lists the exact ordered sequence of called experts and includes the chosen_primary_expert_name when a call occurred. This enforces deterministic routing → first-call fidelity and prevents off-target expert use.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.809961+00:00",
      "updated_at": "2026-06-01T21:54:33.809965+00:00"
    },
    "conflict_and_abstain-00001": {
      "id": "conflict_and_abstain-00001",
      "section": "conflict_and_abstain",
      "content": "Atomic candidate-only routing_summary pre-call + fatal-runtime non-candidate enforcement — For every profile the orchestrator MUST atomically compute a per-profile routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and deterministically chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). Pre-call verify payload.routing_summary == computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call. If ANY pre-call or runtime check fails (including payload.routing_summary mismatch, payload.candidate_experts != profile.candidate_experts, first-called != chosen_primary, any invoked expert ∉ profile.candidate_experts, or ZERO experts invoked when candidate_experts non-empty) TREAT THIS AS A FATAL ROUTING VIOLATION for that profile and IMMEDIATELY perform an atomic replacement of that profile's output with the standardized per-profile abstain JSON EXACTLY: final_label = \"\"; alternative_labels = the ordered profile.candidate_labels; predicted_tissue = \"\"; supporting_genes = the pre-call per-profile intersections if available else []; negative_markers = []; expert_models_used = []; confidence = \"low\"; abstain = true. The abstain.rationale MUST be a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (e.g., \"payload.routing_summary mismatch; attempted call to non-candidate expert 'X'\"). Do NOT accept, synthesize from, reference, or surface any returned expert outputs for that profile, stop further expert calls for that profile, and do not mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.809972+00:00",
      "updated_at": "2026-06-01T21:54:33.809977+00:00"
    },
    "expert_routing-00003": {
      "id": "expert_routing-00003",
      "section": "expert_routing",
      "content": "Fallback-call rule for zero-canonical-marker cases — If for a profile every candidate_expert has intersection(expert.canonical_routing_markers, supplied_ordered_genes) == 0, DO NOT call external/non-candidate experts. If candidate_experts is non-empty, select a fallback primary candidate_expert by preferring canonical_tissue match to supplied tissue context, then by candidate_experts list order, and call that primary once using the standard tool input contract. Follow the usual acceptance, optional single-secondary, and abstain logic after computing intersections; if no expert is accepted after allowed calls, set abstain per conflict_and_abstain-00001. This rule replaces immediate no-call abstention for profiles that have candidate_experts available but no canonical routing marker hits, ensuring experts are invoked and evidence is grounded before abstaining.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.809985+00:00",
      "updated_at": "2026-06-01T21:54:33.809990+00:00"
    },
    "evidence_grounding-00001": {
      "id": "evidence_grounding-00001",
      "section": "evidence_grounding",
      "content": "Atomic routing→first-call→strict-intersection enforcement & fatal per-profile abstain: For every profile (only over profile.candidate_experts) the orchestrator MUST atomically: (A) pre-call compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) and earliest_index and deterministically choose chosen_primary_expert; build a routing_summary (per-candidate intersection_count, earliest_index, chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). (B) Pre-call verify payload.routing_summary == computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; if this fails DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue=\"\", supporting_genes = pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the failure. (C) Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call; at runtime immediately verify the first-returned.expert_name == routing_summary.chosen_primary_expert_name AND that every invoked expert ∈ profile.candidate_experts. If any runtime membership/first-call check fails, TREAT AS A FATAL ROUTING VIOLATION and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (use pre-call intersections if available) with a concise rationale naming the offending expert(s). (D) On a validated returned expert compute computed_supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and computed_negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; record expert_models_used = the exact runtime-ordered invoked experts. (E) Before emitting any non-abstain per-profile final output atomically verify: final.supporting_genes == computed_supporting_genes AND final.negative_markers == computed_negative_markers AND final.predicted_tissue == returned.canonical_tissue AND final.expert_models_used == runtime-ordered invoked experts AND final.final_label maps exactly to one provided profile.candidate_label. If ANY of these equalities fail (including any attempt to add or synthesize supporting_genes/negative_markers from the supplied list or other experts), TREAT THIS AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile and IMMEDIATELY replace/emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). (F) Rationale in any accepted non-abstain output MUST cite ONLY computed_supporting_genes and computed_negative_markers; abstain rationale MUST be a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable. Do not call non-candidate experts, do not mix evidence across profiles, do not emit non-abstain fields prior to a validated expert return, and do not call >2 experts (primary + at most one deterministic secondary per existing routing rules).",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.809996+00:00",
      "updated_at": "2026-06-01T21:54:33.810000+00:00"
    },
    "expert_output_synthesis-00002": {
      "id": "expert_output_synthesis-00002",
      "section": "expert_output_synthesis",
      "content": "Atomic routing_summary→primary-first→strict-intersection synthesis gate: For every profile with non-empty profile.candidate_experts perform one atomic per-profile sequence over ONLY profile.candidate_experts: (1) Pre-call: compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) and earliest_index for each candidate; deterministically choose chosen_primary_expert (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate_experts order). Build a routing_summary listing each candidate's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include that routing_summary VERBATIM in the single-profile outgoing tool payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels) and verify PRE-CALL that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the failure. (2) Call-first fidelity: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert ∈ profile.candidate_experts. If this verification fails, TREAT AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned expert outputs for that profile; stop further calls for that profile; compute supporting_genes = pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON exactly with a concise rationale naming the offending expert(s). (3) Post-return strict grounding: for the validated returned expert compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; record expert_models_used = the exact runtime-ordered list of invoked experts for that profile. Do NOT add, invent, or substitute any genes, canonical markers, or tissue strings. (4) Acceptance & single deterministic secondary: accept (abstain=false) the primary ONLY when BOTH: (A) returned.final_label unambiguously maps exactly to one provided profile.candidate_label AND (B) supporting_genes is non-empty. If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim), append the secondary to expert_models_used in runtime order, and apply the identical post-return grounding and acceptance checks to the secondary only. Do NOT call more than two experts. (5) Final derivation on accept: when an expert (primary or accepted deterministic secondary) is accepted populate final fields STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapped provided candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); rationale must cite ONLY these computed intersections; expert_models_used must equal the runtime-ordered invoked experts; confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (6) Retroactive detection & atomic abstain: before emitting any non-abstain per-profile final output atomically verify that every final field equals the computed intersections/mapping and that final_answer.expert_models_used exactly equals the runtime-ordered call trace. If any final field is not strictly derived from those computed intersections/mapping (including invented labels, tissues, or supporting_genes) OR expert_models_used mismatches runtime trace OR any pre-call/runtime membership check was violated, TREAT THIS AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile: do NOT accept or synthesize from the unauthorized data and IMMEDIATELY replace that profile's output with the standardized per-profile abstain JSON EXACTLY (final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain.rationale MUST be a single concise sentence naming the specific violation and listing offending expert_name(s) when applicable. Do not mix evidence across profiles, do not call non-candidate experts, and do not emit non-abstain final fields prior to a validated accepted expert return.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810008+00:00",
      "updated_at": "2026-06-01T21:54:33.810012+00:00"
    },
    "expert_routing-00004": {
      "id": "expert_routing-00004",
      "section": "expert_routing",
      "content": "expert_routing-00004: Empty-candidate pre-call gating — If candidate_experts is empty for a profile, DO NOT call any experts (including external/non-candidate experts) under any circumstance. Immediately trigger abstention for that profile and populate final_answer fields exactly as follows: final_label = \"\"; alternative_labels = ordered candidate_labels (may be the full provided list); predicted_tissue = \"\"; supporting_genes = []; negative_markers = []; expert_models_used = []; confidence = \"low\"; abstain = true; rationale = \"no candidate_experts provided; no expert called.\" This rule must be applied before any canonical-marker intersections are used to select primaries and prevents unauthorized expert calls or missing abstain fields.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810019+00:00",
      "updated_at": "2026-06-01T21:54:33.810023+00:00"
    },
    "expert_output_synthesis-00003": {
      "id": "expert_output_synthesis-00003",
      "section": "expert_output_synthesis",
      "content": "Atomic per-profile routing→payload→first-call→grounded-synthesis (compact consolidation) — For each profile perform one atomic sequence over ONLY profile.candidate_experts: (1) Pre-call: compute a routing_summary restricted to profile.candidate_experts recording for each candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest matched index (or null) and deterministically choose chosen_primary_expert (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate order). Include this routing_summary VERBATIM in the single-profile tool payload together with profile_id, question_text, supplied_ordered_genes (ordered), task_family/type, grounded_context, and ordered candidate_labels and verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. (2) First-call fidelity: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. At runtime immediately verify first-returned.expert_name == routing_summary.chosen_primary_expert_name AND that every invoked expert ∈ profile.candidate_experts. If ANY pre-call or runtime check fails, TREAT AS A FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from any returned outputs for that profile; stop further expert calls for that profile; compute supporting_genes = the pre-call per-profile intersections (if computed and available) else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections or [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the specific violation and offending expert(s). (3) Post-return grounding & single-secondary: upon a validated returned expert compute computed_supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and computed_negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; record expert_models_used = the exact runtime-ordered invoked experts for that profile. Accept (abstain=false) the primary ONLY when BOTH: (A) returned.final_label unambiguously maps exactly to one provided profile.candidate_label AND (B) computed_supporting_genes is non-empty. If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (include that secondary-call routing_summary VERBATIM), append the secondary to expert_models_used in runtime order, compute intersections for the secondary, and apply identical grounding and acceptance checks to the secondary only. Do NOT call more than two experts. (4) Final derivation on accept: when an expert (primary or accepted secondary) is accepted populate final fields STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapped provided candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = computed_supporting_genes; negative_markers = computed_negative_markers (include field even if empty); rationale must cite ONLY these computed intersections; expert_models_used must equal the runtime-ordered invoked experts for that profile; confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (5) Retroactive fatality & abstain: before emitting any non-abstain per-profile final output atomically verify that every final field equals the computed intersections/mapping and that final_answer.expert_models_used exactly equals the runtime-ordered call trace. If ANY final field would not equal these computed intersections/mapping OR any pre-call/runtime membership check was violated OR expert_models_used mismatches the runtime trace OR more than two experts were invoked OR evidence was mixed across profiles, IMMEDIATELY replace that profile's output with the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) and include a single concise rationale naming the specific violation and offending expert_name(s) when applicable. Under no circumstances call non-candidate experts, mix evidence across profiles, invent markers/tissues/labels, or emit non-abstain final fields prior to a validated accepted expert return.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810030+00:00",
      "updated_at": "2026-06-01T21:54:33.810034+00:00"
    },
    "expert_output_synthesis-00005": {
      "id": "expert_output_synthesis-00005",
      "section": "expert_output_synthesis",
      "content": "Augment expert_output_synthesis-00005 to require atomic equality to the first-invoked expert: Before emitting any non-abstain per-profile final output the orchestrator MUST atomically verify these exact equalities versus the first-invoked (runtime-first-return) expert: (1) final.predicted_tissue == first_invoked_expert.canonical_tissue (verbatim); (2) final.supporting_genes == intersection(first_invoked_expert.supporting_genes, supplied_ordered_genes); and (3) final.negative_markers MUST be present (may be empty) and equal intersection(first_invoked_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). If ANY equality fails (including paraphrased or normalized tissue strings) TREAT THIS AS A FATAL SYNTHESIS VIOLATION for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY. The abstain.rationale MUST be a single concise sentence naming the specific equality failure (e.g., \"predicted_tissue != first_invoked_expert.canonical_tissue\" or \"missing negative_markers field\"). Do not accept, synthesize from, or surface any returned expert outputs for that profile when this violation occurs.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810041+00:00",
      "updated_at": "2026-06-01T21:54:33.810045+00:00"
    },
    "expert_routing-00006": {
      "id": "expert_routing-00006",
      "section": "expert_routing",
      "content": "External-call gating and fallback behavior — If candidate_experts is empty for a profile, DO NOT call any experts and immediately trigger the standardized abstain output for that profile (final_label=\"\", alternative_labels=ordered candidate_labels, predicted_tissue=\"\", supporting_genes=[], negative_markers=[], expert_models_used=[], confidence=\"low\", abstain=true, rationale=\"no candidate_experts provided; no expert called.\"). If candidate_experts is non-empty but none show canonical routing marker intersections, DO NOT call non-candidate experts; instead select and call a fallback primary candidate_expert as described in routing rules (prefer tissue match then list order) and proceed with primary→optional single secondary acceptance and abstain rules. If an attempted call to a non-candidate expert is detected, abort the call and produce the same abstain output. Apply this rule before any tool invocation and do not perform any external calls when candidate_experts is empty.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810051+00:00",
      "updated_at": "2026-06-01T21:54:33.810055+00:00"
    },
    "expert_routing-00007": {
      "id": "expert_routing-00007",
      "section": "expert_routing",
      "content": "expert_routing-00007: Minimum-secondary and explicit-abstain-on-single-candidate — Pre-call compute intersection_count and earliest_index for each candidate_expert per [b1]. If candidate_experts contains exactly one expert for a profile, call that primary per standard tool_input_restructuring. After the expert returns compute supporting_genes = intersection(expert.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(expert.negative_markers, supplied_ordered_genes). If supporting_genes is non-empty apply normal acceptance rules. If supporting_genes is empty AND the pre-call intersection_count between that expert.canonical_routing_markers and supplied_ordered_genes was >0 (i.e., canonical markers were present in the input but not returned by the expert), do NOT invent evidence; set abstain=true and emit the standardized abstain output. The abstain rationale must explicitly list which canonical markers were present in the supplied ordered genes but absent from expert.supporting_genes (e.g., \"checked TPPP3, DNAH5; present in input but not returned by expert\"), state that only one candidate_expert was available, and recommend re-running the query with at least one additional candidate_expert (or broader atlas) to resolve the discrepancy. Populate abstain fields exactly per conflict_and_abstain/expert_output_synthesis templates: final_label=\"\", alternative_labels=ordered candidate_labels, predicted_tissue=\"\", supporting_genes = computed intersections (may be empty), negative_markers = computed intersections (include field even if empty), expert_models_used = [the single called expert], confidence=\"low\", abstain=true.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810062+00:00",
      "updated_at": "2026-06-01T21:54:33.810067+00:00"
    },
    "expert_output_synthesis-00099": {
      "id": "expert_output_synthesis-00099",
      "section": "expert_output_synthesis",
      "content": "Augment expert_output_synthesis-00099 with explicit atomic post-return mapping + required-negative handling: after the routing_summary.chosen_primary_expert_name is invoked and returns, immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name AND returned.expert_name ∈ profile.candidate_experts. Atomically compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Require that returned.final_label unambiguously maps exactly to one of profile.candidate_labels BEFORE acceptance. Apply the following deterministic flow: (1) If mapping succeeds AND supporting_genes non-empty AND (if user_provided_negative_markers present then negative_markers contains every user_provided_negative_marker), accept the primary and populate final fields strictly from the accepted expert's returned fields intersected with supplied_ordered_genes (final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = returned.canonical_tissue verbatim; supporting_genes and negative_markers = the computed intersections; expert_models_used = exact runtime-ordered invoked experts; rationale cites only these intersections; confidence per deterministic rule). (2) If the user_provided_negative_markers are not all present in negative_markers, treat the primary as failed for acceptance; if a permitted deterministic secondary remains, perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim) and repeat these post-return checks for the secondary; otherwise emit the standardized per-profile abstain JSON EXACTLY with supporting_genes = pre-call intersections if required by fatal rules or computed intersections when valid calls occurred, negative_markers = computed intersection (or []), expert_models_used = runtime-ordered called experts (or [] if fatal), confidence = \"low\", abstain = true, and a concise rationale listing the missing expected negative markers. (3) If returned.final_label does not map to a provided candidate_label OR supporting_genes is empty, treat the primary as failed and follow the same single-deterministic-secondary fallback; if no permitted secondary or the secondary also fails, emit the standardized per-profile abstain JSON EXACTLY with a concise rationale naming the failure (e.g., \"returned.final_label not in candidate_labels\" or \"supporting_genes empty\" and list canonical markers present in supplied_ordered_genes but absent from returned.supporting_genes when applicable). (4) Never call or accept outputs from experts not in profile.candidate_experts; any such runtime invocation is a fatal routing violation for that profile and must immediately produce the standardized per-profile abstain JSON exactly. (5) All final supporting_genes and negative_markers MUST be drawn only from intersections with the supplied_ordered_genes; do NOT invent, add, or mix evidence across profiles. This update enforces immediate post-return mapping, exact intersection computation including required negative-marker checks, the single-deterministic-secondary fallback, and mandatory standardized abstain behavior on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810073+00:00",
      "updated_at": "2026-06-01T21:54:33.810077+00:00"
    },
    "conflict_and_abstain-00008": {
      "id": "conflict_and_abstain-00008",
      "section": "conflict_and_abstain",
      "content": "Augment post-call atomic validation to include final_answer ↔ runtime call-trace consistency: after any expert/tool returns, in addition to verifying returned expert_name was present in that profile's candidate_experts and that no expert output was used for the wrong profile, also verify that the final_answer.expert_models_used field exactly equals the ordered sequence of experts actually invoked for that profile (the runtime call trace). If any invoked expert is not a member of profile.candidate_experts OR final_answer.expert_models_used does not exactly match the runtime-invoked ordered experts, treat this as a fatal routing violation for that profile: do NOT accept or synthesize from the unauthorized outputs, and emit the standardized abstain output for that profile with abstain=true, confidence=\"low\", final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue=\"\", supporting_genes = [], negative_markers = [], expert_models_used = [], and a concise rationale naming the offending expert(s) (e.g., \"detected non-candidate expert 'X' used\" or \"final_answer.expert_models_used mismatches runtime-invoked experts\"). Do not attempt further expert calls for that profile and do not mix evidence across profiles when recovering others' outputs.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810084+00:00",
      "updated_at": "2026-06-01T21:54:33.810088+00:00"
    },
    "tool_input_restructuring-00009": {
      "id": "tool_input_restructuring-00009",
      "section": "tool_input_restructuring",
      "content": "Atomic per-profile routing_summary pre-call computation & mandatory first-call fidelity — For every single-profile outgoing tool payload the orchestrator MUST: (A) compute a routing_summary over ONLY profile.candidate_experts recording per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index; deterministically choose routing_summary.chosen_primary_expert_name (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate list order). (B) Include this routing_summary VERBATIM in the single-profile payload together with profile_id, question_text, supplied_ordered_genes (ordered), task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts. Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == the computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections (or [] if not available), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; set the abstain.rationale to a single concise sentence naming the failure (e.g., \"payload.routing_summary mismatch\"). (C) Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime IMMEDIATELY verify that the first-returned.expert_name == routing_summary.chosen_primary_expert_name AND that every invoked expert ∈ profile.candidate_experts. If any runtime membership/first-call check fails (including any invoked non-candidate expert or first-called != chosen_primary), TREAT THIS AS A FATAL ROUTING VIOLATION: do NOT accept, synthesize from, or reference any returned expert outputs for that profile; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY replace/emit the standardized per-profile abstain JSON EXACTLY with fields as above. The abstain.rationale MUST concisely name the specific violation and list offending expert_name(s) (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Do not attempt recovery, do not mix evidence across profiles, and do not accept or surface data from unauthorized experts for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810095+00:00",
      "updated_at": "2026-06-01T21:54:33.810099+00:00"
    },
    "conflict_and_abstain-00010": {
      "id": "conflict_and_abstain-00010",
      "section": "conflict_and_abstain",
      "content": "Augment conflict_and_abstain-00010 with explicit atomic post-orchestration grounding checks: after routing and any allowed expert/tool returns, in addition to existing checks (expert_models_used non-empty and includes the routing_summary.chosen_primary_expert_name), verify atomically that (1) final_answer.predicted_tissue exactly equals the canonical_tissue string reported by the first invoked expert in runtime call order, and (2) final_answer.supporting_genes is a subset of intersection(first_invoked_expert.supporting_genes, supplied_ordered_genes) and final_answer.negative_markers is a subset of intersection(first_invoked_expert.negative_markers, supplied_ordered_genes). Also verify that no final_label, supporting_genes, negative_markers, predicted_tissue, rationale, confidence, or abstain was produced for the profile prior to any valid call when profile.candidate_experts is non-empty. If any of these checks fail (including predicted_tissue mismatch or evidence not strictly subsetted from the invoked expert's intersections), treat this as a fatal routing violation for that profile and emit the standardized per-profile abstain output (do not accept or synthesize from the unauthorized or internally derived fields).",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810106+00:00",
      "updated_at": "2026-06-01T21:54:33.810110+00:00"
    },
    "tool_input_restructuring-000XX": {
      "id": "tool_input_restructuring-000XX",
      "section": "tool_input_restructuring",
      "content": "Retroactive-fatal-no-expert-called: If profile.candidate_experts is non-empty and the runtime call trace for that profile shows ZERO invoked experts (no expert/tool call observed), TREAT THIS AS A FATAL ROUTING VIOLATION for that profile. Immediately and atomically: (A) Do NOT accept, synthesize from, or reference any internally-derived or pre-call heuristics for that profile; (B) Stop any further expert calls for that profile; (C) Compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if available else []; (D) Emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (E) Rationale MUST be a concise sentence naming the violation (e.g., \"no expert called; internal labeling attempted\"). Do not attempt recovery or mix evidence from other profiles when producing this abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810117+00:00",
      "updated_at": "2026-06-01T21:54:33.810121+00:00"
    },
    "tool_input_restructuring-00020": {
      "id": "tool_input_restructuring-00020",
      "section": "tool_input_restructuring",
      "content": "Strengthen atomic per-profile routing_summary gate: For every single-profile tool payload the orchestrator MUST compute a routing_summary over ONLY profile.candidate_experts recording per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). If any candidate has intersection_count>0 the chosen_primary_expert MUST be selected from that subset (tie-break: smallest earliest_index → exact profile.tissue match → candidate list order); only if all candidates have intersection_count==0 may a zero-intersection candidate be chosen (apply existing fallback). Include the computed routing_summary VERBATIM in the outgoing single-profile payload and atomically verify BEFORE any call that: payload.candidate_experts == profile.candidate_experts, payload.routing_summary == the computed routing_summary, and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call. If any pre-call check fails OR at runtime the first-called/returned expert_name != routing_summary.chosen_primary_expert_name OR any invoked expert is not a member of profile.candidate_experts, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept/synthesize from any returned outputs for that profile; compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation and offending expert(s) (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). ",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810128+00:00",
      "updated_at": "2026-06-01T21:54:33.810135+00:00"
    },
    "tool_input_restructuring-00012": {
      "id": "tool_input_restructuring-00012",
      "section": "tool_input_restructuring",
      "content": "When a profile's chosen_primary_expert_name is invoked and returns (post-call) but fails acceptance (including empty supporting_genes), and profile.candidate_experts.size > 1, the orchestrator MUST perform EXACTLY ONE deterministic secondary call to a different candidate_expert selected by the next-largest effective intersection_count (apply the same tie-breaks: earliest_index → tissue match → candidate order). For that secondary call build and send a single-profile payload that includes a routing_summary (restricted to that profile's candidate_experts) listing the computed intersection_count and earliest_index for each candidate_expert and the chosen_primary_expert_name for that SECONDARY call (i.e., the secondary chosen for this call) verbatim. After the secondary returns, compute supporting_genes = intersection(secondary.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(secondary.negative_markers, supplied_ordered_genes) and apply the same acceptance rules. If the secondary is accepted, populate final fields grounded to the secondary's intersections and list expert_models_used in exact call order [primary, secondary]. If the secondary is not accepted (or cannot be called for any allowed reason), emit the standardized per-profile abstain output exactly, and include expert_models_used = the exact ordered list of the called experts (e.g., [primary] or [primary, secondary]) and supporting_genes/negative_markers = the computed intersections (may be empty). Do not call additional experts, do not invent evidence, and do not mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810148+00:00",
      "updated_at": "2026-06-01T21:54:33.810155+00:00"
    },
    "evidence_grounding-000XX": {
      "id": "evidence_grounding-000XX",
      "section": "evidence_grounding",
      "content": "When the user supplies an explicit negative/exclusion marker list for the query, require final negative_markers to be computed as negative_markers = intersection(expert.negative_markers, supplied_ordered_genes, user_provided_negative_markers). If no user-provided negative list is present, fall back to evidence_grounding-00001 behavior (negative_markers = intersection(expert.negative_markers, supplied_ordered_genes)). In all cases include negative_markers (may be empty) in final_answer and cite them in the rationale; never add or invent additional negative markers not present in these intersections.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810163+00:00",
      "updated_at": "2026-06-01T21:54:33.810168+00:00"
    },
    "expert_routing-00013": {
      "id": "expert_routing-00013",
      "section": "expert_routing",
      "content": "Augment per-profile atomic routing to compute a deterministic match_score for every candidate_expert (in addition to intersection_count and earliest_index) and record these scores and any applied filter in routing_summary. Match_score composition (deterministic): +1000 if candidate.canonical_tissue == profile.tissue; +100 * intersection_count; -index_penalty = earliest_index (smaller index better; null -> large penalty); +50 if candidate.metadata.description/tags semantically match candidate_labels or question keywords (e.g., 'ciliated','endothelial'); +200 if a prior accepted tool outcome for this sample/session previously accepted an expert with the same canonical_tissue. Deterministic chosen_primary_expert selection: highest match_score → tie-break smallest earliest_index → tie-break candidate_experts list order. Important exclusion rule: if any candidate_expert has intersection_count > 0, do NOT select as chosen_primary any candidate with intersection_count == 0 (unless all candidates have intersection_count == 0, in which case apply the existing fallback-call rule). Always include the match_scores and which filters were applied (e.g., 'tissue_filtered','description_bonus','prior_outcome_bonus') verbatim in routing_summary. Preserve all existing atomic pre-call/payload checks, single-profile payload contract, single-primary (and at most one deterministic secondary) call rule, and strict post-call intersection grounding/abstain behavior unchanged.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810179+00:00",
      "updated_at": "2026-06-01T21:54:33.810184+00:00"
    },
    "tool_input_restructuring-00021": {
      "id": "tool_input_restructuring-00021",
      "section": "tool_input_restructuring",
      "content": "tool_input_restructuring-00021: Mandatory routing_summary equality check and fatal-abstain on mismatch — For every single-profile tool invocation, the orchestrator MUST compute a routing_summary (per-candidate_expert intersection_count and earliest_index plus chosen_primary_expert_name) and include that exact computed routing_summary verbatim in the outgoing single-profile payload. Before any expert call atomically verify that (A) a routing_summary is present in the payload and (B) payload.routing_summary exactly equals the orchestrator-computed routing_summary. If either check fails (routing_summary missing or payload.routing_summary != computed routing_summary), treat this as a fatal routing violation for that profile: do NOT call any experts for that profile, do NOT accept or synthesize any expert outputs for it, and immediately emit the standardized per-profile abstain output exactly per conflict_and_abstain/expert_output_synthesis templates (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain rationale must concisely name the failure (e.g., \"missing or altered routing_summary in payload; expected computed routing_summary not found\"). Do not attempt further expert calls for that profile and do not mix or reuse evidence from other profiles when producing this abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810192+00:00",
      "updated_at": "2026-06-01T21:54:33.810197+00:00"
    },
    "expert_routing-00014": {
      "id": "expert_routing-00014",
      "section": "expert_routing",
      "content": "Non-candidate-call gating and user-expansion requirement — Before any expert/tool invocation, the orchestrator MUST verify that the intended expert_name is a member of that profile's profile.candidate_experts. If the intended expert is NOT in profile.candidate_experts, DO NOT call the expert. Instead immediately abort the planned call, do NOT accept or synthesize from any outputs that would have come from the unauthorized call, and emit the standardized per-profile abstain output naming the attempted non-candidate expert (concise rationale such as \"attempted call to non-candidate expert 'X'\"). Do not perform further calls for that profile. If calling a non-candidate expert is required, first obtain an explicit user action that expands profile.candidate_experts to include that expert; only after the user-provided expansion may the orchestrator recompute routing_summary and permit a call. This rule prevents unauthorized expert use and ensures abstain outputs are produced atomically when non-candidate calls would otherwise occur.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810205+00:00",
      "updated_at": "2026-06-01T21:54:33.810209+00:00"
    },
    "tool_input_restructuring-00030": {
      "id": "tool_input_restructuring-00030",
      "section": "tool_input_restructuring",
      "content": "Extend tool_input_restructuring-00030 with atomic pre-call/post-return enforcement and standardized fatal-routing abstain: For each single-profile invocation compute a routing_summary for ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index plus chosen_primary_expert_name) and include that routing_summary VERBATIM in the outgoing single-profile payload. Atomically verify pre-call that (A) profile.candidate_experts is non-empty (if empty, follow expert_routing-00004 abstain), (B) payload.routing_summary exactly equals the orchestrator-computed routing_summary, and (C) the intended expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. Do NOT emit any final_label, supporting_genes, negative_markers, predicted_tissue, expert_models_used, confidence, rationale, or abstain for that profile prior to a validated expert return. Invoke exactly routing_summary.chosen_primary_expert_name as the first call for that profile. After the first return immediately verify returned expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. If any pre-call or post-return check fails (missing/altered routing_summary, payload.routing_summary != computed value, chosen_primary_expert_name not in candidate_experts, or first-called/returned expert != chosen_primary_expert_name, or any attempted call to a non-candidate expert) treat this as a fatal routing violation for that profile: do NOT accept, synthesize from, or reference any returned expert outputs for that profile; compute supporting_genes = the pre-call per-profile intersections (intersection of each candidate's canonical routing markers with supplied_ordered_genes) if available else []; and emit the standardized per-profile abstain JSON exactly with fields: final_label = \"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain rationale must concisely name the violation and offending expert(s) (e.g., \"routing_summary missing; attempted call to non-candidate 'X'\" or \"chosen_primary_expert_name 'Y' not invoked; first-called 'Z'\"). Do not attempt further expert calls for that profile and do not mix or reuse evidence from other profiles when producing the abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810215+00:00",
      "updated_at": "2026-06-01T21:54:33.810219+00:00"
    },
    "expert_routing-00015": {
      "id": "expert_routing-00015",
      "section": "expert_routing",
      "content": "Atomic per-profile candidate-only routing_summary enforcement and fatal-abstain on non-candidate calls: For every profile the orchestrator MUST atomically compute a routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload. Before any call verify payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify the first-returned/any-invoked expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. If any pre-call or runtime check fails (including any invoked expert not in profile.candidate_experts, first-called != chosen_primary, or payload mismatch), TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned outputs for that profile; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain rationale MUST concisely name the violation and offending expert(s) (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Do not attempt to recover by calling non-candidate experts, do not mix evidence across profiles, and do not emit any per-profile final fields prior to a validated expert return.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810226+00:00",
      "updated_at": "2026-06-01T21:54:33.810230+00:00"
    },
    "expert_output_synthesis-00016": {
      "id": "expert_output_synthesis-00016",
      "section": "expert_output_synthesis",
      "content": "Augment expert_output_synthesis-00016: After a validated expert return the orchestrator MUST atomically compute computed_supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and computed_negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). The per-profile final supporting_genes and negative_markers fields MUST equal these computed intersections exactly; negative_markers MUST be emitted for every profile (may be an explicit empty list). final_answer.expert_models_used MUST be present and MUST exactly equal the runtime-ordered list of experts actually called for that profile (the runtime call trace). The rationale field MUST cite only computed_supporting_genes and computed_negative_markers. Confidence MUST be set deterministically: 'high' iff returned.reported_confidence == 'high' AND computed_supporting_genes contains ≥1 returned.canonical_routing_marker; else 'moderate' iff computed_supporting_genes non-empty; else 'low'. Do NOT invent, add, or assert any user-provided exclusion markers or any markers/tissues/labels not present in these computed intersections. Any omission or mismatch (including missing negative_markers, expert_models_used not matching the runtime call trace, confidence not set per the rule, or evidence not strictly subsetted from the computed intersections) is a fatal per-profile routing/synthesis violation and MUST produce the standardized per-profile abstain JSON exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810237+00:00",
      "updated_at": "2026-06-01T21:54:33.810240+00:00"
    },
    "expert_routing-00017": {
      "id": "expert_routing-00017",
      "section": "expert_routing",
      "content": "atomic_per-profile_routing_call_and_synthesis: For every profile perform one atomic sequence: (1) Compute routing_summary for ONLY profile.candidate_experts where for each candidate_expert you record intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). Deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match → tie-break candidate_experts order. (2) Build a single-profile tool payload containing profile_id, question_text, supplied_ordered_genes, task_family/task_type, grounded_context, ordered candidate_labels, AND the routing_summary verbatim including chosen_primary_expert_name and that chosen expert's metrics. Atomically verify pre-call that profile.candidate_experts non-empty, payload.routing_summary equals the computed routing_summary, and intended expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. If any pre-call check fails, do NOT call any expert and emit the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections or [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (3) Invoke exactly routing_summary.chosen_primary_expert_name as the first call for that profile. After the expert returns, immediately verify returned expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts; log the runtime-ordered call trace. Any mismatch is a fatal routing violation: do NOT accept returned outputs and emit the standardized abstain output as above naming the offending expert. (4) Compute per-profile intersections from the first-returned expert only: supporting_genes = intersection(first_expert.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(first_expert.negative_markers, supplied_ordered_genes) (if user_provided_negative_markers present include them in the intersection). Set predicted_tissue = first_expert.canonical_tissue exactly and set expert_models_used = [first_expert_name] at this point. (5) Acceptance of primary: accept (abstain=false) only if supporting_genes non-empty AND (A) supporting_genes contains >=1 expert.canonical_routing_marker OR (B) first_expert.final_label unambiguously maps to one provided candidate_label. If accepted, populate final fields strictly from these computed intersections (final_label mapped to candidate_labels, alternative_labels ordered plausible candidates, predicted_tissue, supporting_genes, negative_markers, rationale citing only these intersections, expert_models_used = exact runtime-ordered list, confidence per deterministic rule: 'high' iff first_expert.reported_confidence == 'high' AND supporting_genes contains >=1 canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'). (6) If primary fails acceptance AND profile.candidate_experts.size > 1, perform EXACTLY ONE deterministic secondary call to a different candidate_expert chosen by next-largest intersection_count (apply same tie-breaks). Build and send a single-profile payload for the secondary that includes a routing_summary (per-candidate intersections and chosen_primary_expert_name for that secondary call) verbatim and atomically verify payload equality before calling. After secondary returns compute supporting_genes and negative_markers = intersections with supplied_ordered_genes for the secondary, append secondary to expert_models_used (runtime order), set predicted_tissue = secondary.canonical_tissue exactly, and apply the same acceptance and confidence rules. (7) If neither called expert is accepted, or any deviation from the required pre-call/payload/first-call/allowed-expert rules occurs, emit the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = computed intersections (may be empty) or pre-call intersections per fatal rules, negative_markers = computed intersections or [], expert_models_used = exact runtime-ordered called experts (or [] if no valid call), confidence = \"low\", abstain = true) with a concise rationale naming the violated requirement or offending expert. (8) Never call non-candidate experts, never call more than two experts per profile, never mix evidence across profiles, never invent markers or tissues (all reported supporting_genes, negative_markers, and predicted_tissue must be subsets of the called expert's returned fields intersected with supplied_ordered_genes), and always list expert_models_used exactly as the runtime-ordered sequence of invoked experts.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810247+00:00",
      "updated_at": "2026-06-01T21:54:33.810251+00:00"
    },
    "evidence_grounding-00003": {
      "id": "evidence_grounding-00003",
      "section": "evidence_grounding",
      "content": "Mandatory-expert_models_used-for-accept: For any profile with non-empty profile.candidate_experts, if the orchestration accepts (abstain=false) a final output for that profile then final_answer.expert_models_used MUST be present and MUST exactly equal the runtime-ordered list of experts actually invoked for that profile (and therefore non-empty). If final_answer.abstain == false but expert_models_used is missing or empty or does not match the runtime call trace, TREAT THIS AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile: do NOT accept or synthesize the output and emit the standardized per-profile abstain JSON EXACTLY (final_label = \"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation (e.g., \"missing or empty expert_models_used for accepted output\").",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810257+00:00",
      "updated_at": "2026-06-01T21:54:33.810261+00:00"
    },
    "expert_routing-00018": {
      "id": "expert_routing-00018",
      "section": "expert_routing",
      "content": "Mandatory per-profile candidate-only routing, first-call, grounding, acceptance, and confidence mapping — For every profile with non-empty profile.candidate_experts compute for ONLY those candidate_experts: intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). Deterministically choose a single chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match to profile.tissue → tie-break candidate_experts list order. Build a routing_summary that lists each candidate_expert's intersection_count and earliest_index and includes chosen_primary_expert_name and that chosen expert's metrics; include this routing_summary VERBATIM in the single-profile tool payload. Atomically verify pre-call that profile.candidate_experts is non-empty and that the intended expert_name equals routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts; if this check fails, do NOT call any expert and emit the standardized per-profile abstain output. Invoke exactly routing_summary.chosen_primary_expert_name as the first call for that profile. After the first-returned expert is received, immediately verify returned expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts; any mismatch is a fatal routing violation and must produce the standardized per-profile abstain output. For the accepted expert (primary or at most one deterministic secondary, selected by the next-largest intersection_count using the same tie-breaks), compute supporting_genes = intersection(expert.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(expert.negative_markers, supplied_ordered_genes) (if a user_provided negative list exists include it in the intersection). Set predicted_tissue = expert.canonical_tissue exactly and record expert_models_used as the exact runtime-ordered list of called experts. Accept the primary (abstain=false) only if supporting_genes is non-empty AND (A) supporting_genes contains >=1 expert.canonical_routing_marker OR (B) expert.final_label unambiguously maps to one provided candidate_label. If primary fails acceptance and profile.candidate_experts.size>1, perform EXACTLY ONE deterministic secondary call as above; if the secondary also fails acceptance, emit the standardized per-profile abstain output. Confidence must be set deterministically: 'high' iff expert.reported_confidence == 'high' AND supporting_genes contains >=1 expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. Never call non-candidate experts, never call more than two experts per profile, never invent or add markers or tissues (all reported supporting_genes, negative_markers, and predicted_tissue must be subsets of the called expert's returned fields intersected with supplied_ordered_genes), and never mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810270+00:00",
      "updated_at": "2026-06-01T21:54:33.810276+00:00"
    },
    "evidence_grounding-00010": {
      "id": "evidence_grounding-00010",
      "section": "evidence_grounding",
      "content": "evidence_grounding-00010: Atomic routing→call→no-invention enforcement — For every profile with non-empty profile.candidate_experts perform this atomic sequence and emit the standardized per-profile final output: (A) Pre-call: compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) and earliest_index for each candidate_expert (ONLY profile.candidate_experts). Deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match → tie-break candidate_experts list order. Build a routing_summary listing each candidate_expert's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include this routing_summary VERBATIM in the single-profile tool payload. Atomically verify payload.routing_summary equals the computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts; if any pre-call check fails, do NOT call any experts and emit the standardized per-profile abstain output. (B) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first call for that profile. (C) Post-return grounding: immediately verify returned expert_name == routing_summary.chosen_primary_expert_name and is in profile.candidate_experts; compute supporting_genes = intersection(first_expert.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(first_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Set predicted_tissue = first_expert.canonical_tissue exactly and expert_models_used = [first_expert_name]. Do NOT add, invent, substitute, or generalize any supporting_genes, negative_markers, or predicted_tissue beyond these intersections and verbatim canonical_tissue. (D) Acceptance/secondary/abstain: accept (abstain=false) the primary ONLY if supporting_genes non-empty AND (A) supporting_genes contains >=1 expert.canonical_routing_marker OR (B) first_expert.final_label unambiguously maps to a provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size>1, perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (build/send a single-profile payload with its routing_summary verbatim), append secondary to expert_models_used in runtime order, compute intersections for the secondary, and apply the same acceptance rules. If neither called expert is accepted, emit the standardized per-profile abstain output. (E) Single-candidate explicit abstain: if profile.candidate_experts.size==1 and pre-call intersection_count>0 but the single called expert returns supporting_genes empty, do NOT invent evidence; set abstain=true and include in the rationale which canonical routing markers were present in the supplied_ordered_genes but absent from expert.supporting_genes. (F) Deterministic confidence mapping: set confidence = 'high' iff expert.reported_confidence == 'high' AND supporting_genes contains >=1 expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (G) Failure handling: any deviation from these requirements (missing/altered routing_summary, first-called expert != chosen_primary_expert_name, use of non-candidate expert, evidence not strictly subsetted from the called expert's returned intersections, invented markers/tissues, or expert_models_used mismatch with runtime trace) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation. This rule enforces a single atomic contract that prevents post-call invention, ensures required abstain behavior and explicit missing-canonical-marker reporting, and binds final outputs exactly to the called expert(s).",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810284+00:00",
      "updated_at": "2026-06-01T21:54:33.810288+00:00"
    },
    "expert_routing-00019": {
      "id": "expert_routing-00019",
      "section": "expert_routing",
      "content": "atomic_candidate-only_routing_and_grounded_acceptance: For every profile perform one atomic sequence restricted to profile.candidate_experts only: (A) Pre-call: compute for each candidate_expert intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). Deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match to profile.tissue → tie-break candidate_experts list order. Build a routing_summary listing each candidate_expert's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include this routing_summary VERBATIM in the single-profile tool payload. Atomically verify pre-call that profile.candidate_experts is non-empty, payload.routing_summary equals the computed routing_summary, and the intended expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. If any pre-call check fails, do NOT call any expert and emit the standardized per-profile abstain output (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections or [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (B) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first call for that profile; never call non-candidate experts. (C) Post-return grounding: immediately verify returned expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts; log runtime call order. Compute supporting_genes = intersection(first_expert.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(first_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Set predicted_tissue = first_expert.canonical_tissue exactly and expert_models_used = [first_expert_name]. Do NOT add, invent, or substitute any markers or tissue strings. (D) Acceptance/secondary/abstain: accept (abstain=false) the primary ONLY if supporting_genes non-empty AND (A) supporting_genes contains >=1 expert.canonical_routing_marker OR (B) first_expert.final_label unambiguously maps to one provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size>1, perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (apply same tie-breaks), include its routing_summary verbatim in its single-profile payload, append secondary to expert_models_used, compute intersections for the secondary, and apply the same acceptance rules. If neither called expert is accepted, emit the standardized per-profile abstain output. For a single-candidate profile where pre-call intersection_count>0 but the single called expert returns supporting_genes empty, abstain and include in the rationale which canonical routing markers were present in the supplied_ordered_genes but absent from expert.supporting_genes. (E) Confidence mapping: set confidence = 'high' iff expert.reported_confidence == 'high' AND supporting_genes contains >=1 expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (F) Violations: any deviation from this atomic contract (missing/altered routing_summary, first-called != chosen_primary, use of non-candidate expert, evidence not strictly subsetted from called expert's returned intersections, invented markers/tissues, or expert_models_used mismatch with runtime trace) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain output exactly with a concise rationale naming the violation. This rule enforces candidate-only routing, per-call payload grounding, strict post-call intersection grounding, deterministic acceptance/secondary behavior, and the required final fields or standardized abstain.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810294+00:00",
      "updated_at": "2026-06-01T21:54:33.810298+00:00"
    },
    "expert_output_synthesis-00020": {
      "id": "expert_output_synthesis-00020",
      "section": "expert_output_synthesis",
      "content": "Append an atomic tissue-consistency & negative_marker fatality clause to expert_output_synthesis-00020: After performing the standard routing→call→post-return grounding and computing supporting_genes and negative_markers intersections, if the profile includes an explicit tissue/context (profile.tissue or expected_tissue) AND the accepted_expert.canonical_tissue does NOT exactly equal that profile.tissue, then: (1) If there exists another candidate_expert in profile.candidate_experts whose canonical_tissue exactly equals profile.tissue, perform EXACTLY ONE deterministic secondary call to that next-ranked tissue-matching candidate (send its routing_summary verbatim) and apply the same post-return grounding and acceptance rules; (2) If no candidate_expert matches profile.tissue, DO NOT remap or normalize the accepted_expert.canonical_tissue—treat the mismatch as a reason to abstain only if the acceptance criteria would otherwise be ambiguous (i.e., supporting_genes empty or final_label does not unambiguously map to a provided candidate_label); (3) Missing the negative_markers field in the per-profile final output (or emitting negative_markers not equal to the computed intersection) is a fatal synthesis violation for that profile and MUST produce the standardized per-profile abstain output exactly. All other atomic requirements remain: verbatim predicted_tissue = accepted_expert.canonical_tissue, supporting_genes/negative_markers = computed intersections only, expert_models_used = exact runtime-ordered called experts, deterministic confidence mapping, and single-secondary fallback. Any deviation from these checks is a fatal routing/synthesis violation and MUST emit the standardized per-profile abstain output exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810305+00:00",
      "updated_at": "2026-06-01T21:54:33.810309+00:00"
    },
    "expert_output_synthesis-00021": {
      "id": "expert_output_synthesis-00021",
      "section": "expert_output_synthesis",
      "content": "Augment expert_output_synthesis-00021 (atomic per-profile final-validation gate) with mandatory top-level profile_labels reconciliation: before returning any multi-profile response, atomically verify that the overall response contains a profile_labels mapping and that for every profile where abstain==false the entry profile_labels[profile_id] exactly equals that profile's final_label. If profile_labels is missing or any profile's profile_labels[profile_id] != final_label (including absent keys), treat this as a fatal routing/synthesis violation for the affected profile(s): immediately replace each affected profile's emitted output with the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain.rationale MUST be a single concise sentence naming the violated check (e.g., \"missing or mismatched profile_labels mapping for profile 'X'\"). Do not attempt recovery, do not mix evidence across profiles, and atomically verify replacement before returning the overall response.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810316+00:00",
      "updated_at": "2026-06-01T21:54:33.810320+00:00"
    },
    "conflict_and_abstain-00020": {
      "id": "conflict_and_abstain-00020",
      "section": "conflict_and_abstain",
      "content": "Strengthen conflict_and_abstain-00020 with mandatory atomic replacement and final-validation: If profile.candidate_experts == [] then BEFORE accepting any outputs the orchestrator MUST inspect the runtime call-trace for that profile. If any expert was invoked/returned for that profile, TREAT THIS AS A FATAL ROUTING VIOLATION and perform these atomic steps: (1) Discard and DO NOT accept, synthesize from, or reference any returned expert outputs for that profile; (2) Discard any partial or previously-emitted per-profile fields (do not merge or preserve prose/evidence); (3) IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (4) Rationale MUST be the concise sentence: \"attempted call to expert '<expert_name>' when profile.candidate_experts empty\" listing all offending expert_name(s) if multiple; (5) Before returning the overall orchestrated response, atomically validate that the affected profile's final output exactly matches the standardized abstain JSON above; if any required abstain field is missing or incorrect, replace the profile output with the exact standardized abstain JSON. This atomic enforcement prevents partial acceptance, evidence leakage from unauthorized calls, and ensures uniform, replaceable abstain outputs for empty-candidate fatal violations.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810326+00:00",
      "updated_at": "2026-06-01T21:54:33.810330+00:00"
    },
    "expert_output_synthesis-00022": {
      "id": "expert_output_synthesis-00022",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-00022: Mandatory explicit negative_markers emission — For every profile, after accepting an expert (primary or permitted secondary) compute negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Always include negative_markers in the per-profile final output (may be an explicit empty list) and cite these negative_markers in the rationale. When emitting the standardized per-profile abstain output (including fatal-routing or post-call abstains), include negative_markers computed as above (or [] if not computable). If the final per-profile output would omit negative_markers or include markers not strictly from the computed intersection, treat this as a synthesis violation for that profile and emit the standardized per-profile abstain output exactly, with a concise rationale naming the violation (e.g., \"missing negative_markers\"). This rule is atomic per-profile and does not permit invention, cross-profile mixing, or omission of negative_markers.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810338+00:00",
      "updated_at": "2026-06-01T21:54:33.810341+00:00"
    },
    "tool_input_restructuring-00023": {
      "id": "tool_input_restructuring-00023",
      "section": "tool_input_restructuring",
      "content": "Augment tool_input_restructuring-00023 with an explicit retroactive-fatal-routing-summary check: If at runtime any single-profile outgoing payload either (A) is missing a routing_summary, (B) payload.routing_summary != the orchestrator-computed routing_summary for that profile, or (C) payload.candidate_experts != profile.candidate_experts, AND an expert call was nevertheless attempted or observed, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile. Immediately and atomically do all of the following for the affected profile: (1) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (2) Stop any further expert calls for that profile; (3) Compute supporting_genes = the pre-call per-profile intersections if available else []; (4) Emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (5) The abstain rationale MUST concisely name the violation and list the attempted/offending expert_name(s) (e.g., \"missing routing_summary in payload; attempted call to non-candidate expert 'X'\"). Do not attempt recovery or mix evidence from other profiles when producing this abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810348+00:00",
      "updated_at": "2026-06-01T21:54:33.810352+00:00"
    },
    "conflict_and_abstain-00024": {
      "id": "conflict_and_abstain-00024",
      "section": "conflict_and_abstain",
      "content": "Augment conflict_and_abstain-00024 with mandatory atomic post-call replacement: In addition to treating any runtime invocation of an expert not in profile.candidate_experts as a fatal routing violation, the orchestrator MUST immediately perform an atomic replacement of that profile's output: (A) discard and do NOT accept, synthesize from, or reference any returned outputs for that profile (even if any final fields were previously emitted); (B) stop any further expert calls for that profile; (C) compute supporting_genes = the pre-call per-profile intersections if available else []; (D) replace the profile's output with the standardized per-profile abstain JSON EXACTLY: final_label = \"\", alternative_labels = the ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (E) the abstain rationale MUST concisely name the violation and list the offending non-candidate expert_name(s) (e.g., \"attempted call to non-candidate expert 'X'\"); and (F) atomically verify before returning the overall orchestrated response that the affected profile's output equals this standardized abstain JSON (replace any differing fields). Do not attempt recovery, do not mix evidence across profiles, and do not accept or surface any data returned from the unauthorized expert under any circumstances.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810359+00:00",
      "updated_at": "2026-06-01T21:54:33.810363+00:00"
    },
    "conflict_and_abstain-00025": {
      "id": "conflict_and_abstain-00025",
      "section": "conflict_and_abstain",
      "content": "conflict_and_abstain-00030: Fatal-abstain-on-post-call-empty-primary-without-secondary — If the routing_summary.chosen_primary_expert_name is invoked and returns but the computed supporting_genes = intersection(first_expert.supporting_genes, supplied_ordered_genes) is empty and no permitted deterministic secondary call has yet been performed (or the profile had only one candidate), the orchestrator MUST NOT: (A) call any non-candidate expert, (B) synthesize or invent supporting_genes/negative_markers/tissue/label from the supplied gene list or other experts, or (C) emit a definitive final_label for that profile. Instead the orchestrator MUST either (1) if profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert per existing routing tie-breaks (including sending that secondary's routing_summary verbatim) and then apply the normal grounding and acceptance rules; OR (2) if no secondary exists or the secondary also yields empty supporting_genes, immediately emit the standardized per-profile abstain output exactly: final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections (or [] if not computable), negative_markers = [], expert_models_used = [] (if a fatal-routing mismatch) or the exact runtime-ordered called experts (if valid calls occurred), confidence = \"low\", abstain = true. Any attempt to call or use a non-candidate expert or to produce a non-abstain final output for that profile after empty primary evidence is a fatal routing violation and MUST produce the standardized per-profile abstain output (name the violation and offending expert if applicable). Rationale in the abstain output must list which canonical routing markers were present in the supplied_ordered_genes but absent from the first_expert.supporting_genes when applicable.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810372+00:00",
      "updated_at": "2026-06-01T21:54:33.810378+00:00"
    },
    "conflict_and_abstain-00026": {
      "id": "conflict_and_abstain-00026",
      "section": "conflict_and_abstain",
      "content": "conflict_and_abstain-00030: Mandatory per-profile-abstain (no global substitute) — If any routing or synthesis violation is detected for a profile (including but not limited to: missing/altered per-profile routing_summary in the outgoing payload; payload.routing_summary != computed routing_summary; routing_summary.chosen_primary_expert_name not in profile.candidate_experts; first-called/returned expert_name != routing_summary.chosen_primary_expert_name; any invoked expert not in profile.candidate_experts; post-call grounding mismatch; or evidence-mixing across profiles), the orchestrator MUST: (1) immediately emit the standardized per-profile abstain output EXACTLY for that affected profile with these fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (2) include a concise rationale naming the specific violation and offending expert(s) (e.g., \"missing routing_summary in payload\" or \"attempted call to non-candidate expert 'X'\"); (3) stop any further expert calls for that profile; and (4) NOT substitute a single/global/aggregate abstain instead of emitting one complete standardized abstain output per affected profile. Orchestrator may continue to process and emit independent per-profile outputs for other profiles that did not experience violations. This rule is atomic and mandatory and prevents missing required per-profile fields and evidence mixing caused by global-abstain shortcuts.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810388+00:00",
      "updated_at": "2026-06-01T21:54:33.810393+00:00"
    },
    "expert_routing-00027": {
      "id": "expert_routing-00027",
      "section": "expert_routing",
      "content": "Per-profile atomic routing_summary → first-call → grounded synthesis contract: For every profile compute only over profile.candidate_experts the per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). Deterministically select chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match to profile.tissue → tie-break candidate_experts list order. Build a routing_summary listing each candidate_expert's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include this routing_summary VERBATIM in the single-profile outgoing tool payload (payload must also contain profile_id, question_text, supplied_ordered_genes, task_family/task_type, grounded_context, and ordered candidate_labels). Atomically verify pre-call that profile.candidate_experts non-empty, payload.routing_summary equals the computed routing_summary, and intended expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. If any pre-call check fails, do NOT call any experts and emit the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. Invoke exactly routing_summary.chosen_primary_expert_name as the first call for that profile. After the first expert returns immediately verify returned expert_name == routing_summary.chosen_primary_expert_name and is in profile.candidate_experts; log runtime call order. Compute supporting_genes = intersection(first_expert.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(first_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = first_expert.canonical_tissue verbatim and expert_models_used = [first_expert_name]. Accept the primary (abstain=false) ONLY if supporting_genes non-empty AND (A) supporting_genes contains >=1 first_expert.canonical_routing_marker OR (B) first_expert.final_label unambiguously maps to one provided candidate_label. If the primary fails acceptance AND profile.candidate_experts.size>1, perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (apply same tie-breaks), send a single-profile payload that includes that secondary-call routing_summary verbatim, append secondary to expert_models_used in runtime order, compute supporting_genes and negative_markers for the secondary as intersections with supplied_ordered_genes, and apply the same acceptance rules. If neither called expert is accepted, emit the standardized per-profile abstain output exactly. When accepting an expert, populate final fields strictly from the accepted expert and the computed intersections only: final_label = accepted_expert.final_label mapped to one of the provided candidate_labels; alternative_labels = ordered candidate_labels; predicted_tissue = accepted_expert.canonical_tissue verbatim; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); expert_models_used = exact runtime-ordered list of called experts for that profile; rationale = concise statement citing only the computed supporting_genes and negative_markers intersections (when abstaining due to empty supporting_genes explicitly list which canonical routing markers were present in the supplied_ordered_genes but absent from accepted_expert.supporting_genes); confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains >=1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. Any deviation from this atomic contract (missing/altered routing_summary in payload, first-called expert != routing_summary.chosen_primary_expert_name, invocation of any non-candidate expert, evidence not strictly subsetted from the called expert's returned intersections, mixing evidence across profiles, omission of any required final field, or final expert_models_used mismatch with runtime call trace) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain output exactly with a concise rationale naming the violation. Never call more than two experts per profile, never invent or add markers or tissues, and never mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810401+00:00",
      "updated_at": "2026-06-01T21:54:33.810405+00:00"
    },
    "conflict_and_abstain-00028": {
      "id": "conflict_and_abstain-00028",
      "section": "conflict_and_abstain",
      "content": "retroactive-fatal-routing-abstain-on-unauthorized-call: If at runtime any per-profile pre-call contract was violated (e.g., routing_summary missing or payload.routing_summary != computed routing_summary; intended expert_name not in profile.candidate_experts; or profile.candidate_experts empty) AND an expert/tool was nevertheless invoked for that profile (the runtime call trace shows one or more expert calls), TREAT THIS AS A FATAL ROUTING VIOLATION for that profile. Immediately and atomically do all of the following: (A) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile (ignore all expert-returned supporting_genes, negative_markers, predicted_tissue, final_label, confidence, or rationale). (B) Stop any further expert calls for that profile. (C) Compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if those pre-call intersections were computed and available, else supporting_genes = []. (D) Emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. (E) Rationale MUST be a concise sentence naming the contractual violation and listing the offending expert_name(s) that were invoked (e.g., \"routing_summary missing; attempted call to non-candidate expert 'X'\"). Do not substitute a freeform or partial abstain; the standardized per-profile abstain JSON must be returned and no attempt to recover or synthesize from the unauthorized expert outputs is allowed for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810412+00:00",
      "updated_at": "2026-06-01T21:54:33.810416+00:00"
    },
    "tool_input_restructuring-00029": {
      "id": "tool_input_restructuring-00029",
      "section": "tool_input_restructuring",
      "content": "Augment the atomic per-profile routing_summary → payload → first-call contract with mandatory post-return grounding and fatal standardized per-profile abstain: For every single-profile payload compute the routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) and include that routing_summary VERBATIM in the payload together with profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts. Atomically verify PRE-CALL that payload.routing_summary equals the computed routing_summary and that intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call when profile.candidate_experts is non-empty. At runtime immediately verify that a) at least one expert was invoked, b) the first-returned.expert_name == routing_summary.chosen_primary_expert_name, and c) every invoked expert ∈ profile.candidate_experts. If any pre-call or runtime check fails (including zero experts invoked or any invoked non-candidate or first-called != chosen_primary), TREAT AS A FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from any returned outputs for that profile; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the specific violation (examples: \"no expert called; internal labeling attempted\" or \"first-called 'X' != chosen_primary 'Y'\" or \"attempted call to non-candidate expert 'Z'\"). After a validated expert return compute computed_supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and computed_negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly and set expert_models_used = the exact runtime-ordered invoked experts for that profile. Before emitting any non-abstain per-profile final output atomically verify final.supporting_genes == computed_supporting_genes AND final.negative_markers == computed_negative_markers AND final.predicted_tissue == returned.canonical_tissue AND final.expert_models_used == runtime-ordered invoked experts AND final.final_label maps exactly to one provided profile.candidate_label. If any of these equalities fails, IMMEDIATELY replace that profile's output with the standardized per-profile abstain JSON EXACTLY (use pre-call intersections if available) and a concise rationale naming the violated check. Do not call more than two experts for a profile (primary + at most one deterministic secondary), do not mix evidence across profiles, and do not emit non-abstain final fields prior to a validated expert return.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810423+00:00",
      "updated_at": "2026-06-01T21:54:33.810427+00:00"
    },
    "expert_output_synthesis-00030": {
      "id": "expert_output_synthesis-00030",
      "section": "expert_output_synthesis",
      "content": "runtime-grounded-atomic-synthesis: For each profile perform one atomic routing→call→synthesis sequence: (A) Pre-call: compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) and earliest_index; deterministically choose chosen_primary_expert (highest intersection_count → tie-break earliest_index → tissue match → candidate order). Build routing_summary listing each candidate's intersection_count and earliest_index and chosen_primary_expert_name; include this routing_summary VERBATIM in the single-profile payload and verify payload.candidate_experts == profile.candidate_experts before calling. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call. (B) Post-return grounding: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and is a profile.candidate_expert. Compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Set predicted_tissue = returned.canonical_tissue verbatim and expert_models_used = runtime-ordered invoked experts. (C) Acceptance/secondary: accept (abstain=false) only if supporting_genes non-empty AND (1) supporting_genes contains >=1 returned.canonical_routing_marker OR (2) returned.final_label unambiguously maps to a provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (include its routing_summary verbatim) and repeat steps (B)-(C) once. (D) Deterministic confidence mapping: set confidence = 'high' iff returned.reported_confidence == 'high' AND supporting_genes contains >=1 returned.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (E) Evidence non-invention & isolation: final supporting_genes and negative_markers MUST be the computed intersections above; do NOT add any genes from the supplied list, domain knowledge, or other experts, and never mix evidence across profiles. (F) Fatal violations and abstain: any pre-call or post-call mismatch (missing/altered routing_summary, payload.candidate_experts mismatch, first-called expert != chosen_primary_expert_name, use of non-candidate expert, supporting_genes/negative_markers not subsetted from the computed intersections, or evidence mixed across experts/profiles) MUST produce the standardized per-profile abstain output exactly (final_label = \"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810434+00:00",
      "updated_at": "2026-06-01T21:54:33.810438+00:00"
    },
    "expert_output_synthesis-00023": {
      "id": "expert_output_synthesis-00023",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-00023: Atomic pre-call → first-call → post-return synthesis contract — For every profile perform one atomic sequence: (A) Pre-call: compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) for ONLY profile.candidate_experts; deterministically choose chosen_primary_expert by the existing tie-breaks; build a routing_summary (per-candidate intersection_count and earliest_index plus chosen_primary_expert_name) and include it VERBATIM in the single-profile payload. Verify payload.candidate_experts exactly equals profile.candidate_experts and payload.routing_summary equals the computed routing_summary before calling. Do NOT call non-candidate experts. (B) First-call: invoke exactly routing_summary.chosen_primary_expert_name as the first expert for that profile. (C) Post-return atomic verification: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that it is a member of profile.candidate_experts. If this verification fails, TREAT AS FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from any returned outputs for that profile; compute supporting_genes = pre-call intersections if available else []; and emit the standardized per-profile abstain output with a concise rationale naming the violation and offending expert. (D) Evidence grounding: when the allowed expert returns and passes verification, compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Set predicted_tissue = returned.canonical_tissue verbatim and expert_models_used = the exact runtime-ordered list of called experts for that profile. Do NOT add, invent, or mix genes or tissues. (E) Acceptance & confidence: accept (abstain=false) only if supporting_genes non-empty AND (1) supporting_genes contains >=1 returned.canonical_routing_marker OR (2) returned.final_label unambiguously maps to a provided candidate_label. If primary fails and profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim) and apply the same checks and grounding; otherwise emit standardized per-profile abstain. Set confidence = 'high' iff returned.reported_confidence == 'high' AND supporting_genes contains >=1 returned.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (F) Fatal violations: any deviation from these pre-call/payload/first-call/post-return/evidence-grounding requirements (payload.candidate_experts mismatch, missing/altered routing_summary, first-called/returned expert != chosen_primary_expert_name, invocation of non-candidate expert, final fields not strictly subsetted from the computed intersections, omission of negative_markers, or evidence-mixing across profiles) MUST immediately produce the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check. This rule enforces atomic routing fidelity, strict intersection grounding, verbatim tissue reporting, required negative_markers, deterministic confidence mapping, and immediate abstain on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810446+00:00",
      "updated_at": "2026-06-01T21:54:33.810450+00:00"
    },
    "expert_output_synthesis-00050": {
      "id": "expert_output_synthesis-00050",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-00050 (mandatory post-call strict-intersection & no-invention rule): After any allowed expert returns for a profile the orchestrator MUST perform these atomic post-call steps before emitting that profile's final output: (A) verify returned.expert_name == routing_summary.chosen_primary_expert_name and that it is in profile.candidate_experts; (B) compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); (C) final supporting_genes and negative_markers MUST equal these computed intersections exactly (no addition, substitution, or invention of genes, canonical markers, or tissue strings from domain knowledge or other experts); (D) always include the negative_markers field in the per-profile final output (may be an explicit empty list) and restrict the rationale to cite only the computed supporting_genes and negative_markers intersections; (E) if supporting_genes is empty then do NOT accept the expert: either perform EXACTLY ONE deterministic secondary call per existing routing rules (if permitted) or emit the standardized per-profile abstain output exactly, and when abstaining explicitly list which canonical routing markers were present in supplied_ordered_genes but absent from returned.supporting_genes; (F) final_answer.expert_models_used MUST equal the exact runtime-ordered list of invoked experts for that profile; (G) any deviation from this post-call intersection grounding or any attempt to invent markers/tissues is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain output exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810457+00:00",
      "updated_at": "2026-06-01T21:54:33.810461+00:00"
    },
    "tool_input_restructuring-00031": {
      "id": "tool_input_restructuring-00031",
      "section": "tool_input_restructuring",
      "content": "tool_input_restructuring-NEW-ATOMIC_ROUTING_AND_GROUNDING: For each profile perform one atomic pre-call → call → post-return sequence. Pre-call: compute routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index) and deterministically choose chosen_primary_expert; include this routing_summary VERBATIM in the single-profile payload and verify payload.candidate_experts == profile.candidate_experts and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any expert and emit the standardized per-profile abstain output exactly. Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call. Post-return grounding: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and ∈ profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; set final_label = returned.final_label mapped verbatim to one of the provided candidate_labels; set expert_models_used = the exact runtime-ordered list of called experts. Acceptance and confidence: accept (abstain=false) only if supporting_genes non-empty AND (A) supporting_genes contains ≥1 returned.canonical_routing_marker OR (B) returned.final_label unambiguously maps to a provided candidate_label. Set confidence deterministically: 'high' iff returned.reported_confidence == 'high' AND supporting_genes contains ≥1 returned.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. Fatal checks & abstain: if any verification fails (payload mismatch, first-called ≠ chosen_primary, any invoked expert not in profile.candidate_experts, final fields would require using evidence not present in the computed intersections, missing negative_markers field, or any cross-profile evidence mixing), treat as a fatal routing/synthesis violation and emit the standardized per-profile abstain output exactly. Never call non-candidate experts, never invent or add supporting_genes/negative_markers/tissue/labels, and never mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810467+00:00",
      "updated_at": "2026-06-01T21:54:33.810471+00:00"
    },
    "conflict_and_abstain-00032": {
      "id": "conflict_and_abstain-00032",
      "section": "conflict_and_abstain",
      "content": "fatal-missing-or-mismatched-routing_summary-standardized-abstain: If for any profile any of the following pre-call or runtime contract checks fails — (A) the per-profile payload is missing a routing_summary OR payload.routing_summary != the orchestrator-computed routing_summary (verbatim), (B) payload.candidate_experts != profile.candidate_experts, (C) the intended expert_name != routing_summary.chosen_primary_expert_name, OR (D) at runtime the first-called expert_name != routing_summary.chosen_primary_expert_name OR any invoked expert is not a member of profile.candidate_experts — TREAT THIS AS A FATAL ROUTING VIOLATION for that profile. Immediately and atomically do NOT accept, synthesize from, or reference any returned expert outputs for the affected profile; stop any further expert calls for that profile; compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if available else []; and EMIT the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = the ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain rationale MUST be a concise sentence naming the specific contract violation and list offending expert_name(s) when applicable (e.g., \"missing routing_summary in payload\" or \"first-called 'X' != chosen_primary 'Y'\"). Do not attempt recovery, further calls, or to mix evidence from other profiles when producing this abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810477+00:00",
      "updated_at": "2026-06-01T21:54:33.810481+00:00"
    },
    "expert_routing-000XX": {
      "id": "expert_routing-000XX",
      "section": "expert_routing",
      "content": "expert_routing-000XX (prefer tissue-matched then functionally-matched experts): Pre-call, compute per-candidate intersections and earliest_index per existing rules. Then narrow the primary-selection set as follows (atomic, applied before chosen_primary_expert determination): (A) If profile.tissue is present, restrict selection to candidate_experts whose canonical_tissue exactly equals profile.tissue; if this subset is non-empty continue deterministic chosen_primary_expert selection within it. (B) If profile.tissue is absent OR the tissue-filtered subset is empty, AND any provided candidate_labels contain lineage keywords (e.g., 'monocyte', 'macrophage', 'dendritic'), preferentially restrict selection to candidate_experts whose available metadata indicates immune or myeloid specialization (e.g., 'immune','myeloid' tags or equivalent canonical specialization field). If such specialized candidates exist, choose the chosen_primary_expert deterministically within that subset. (C) If neither (A) nor (B) yields candidates, revert to the standard deterministic selection over all profile.candidate_experts. DO NOT call non-candidate experts at any point; if profile.candidate_experts is empty follow existing empty-candidate abstain rules. Record the applied filter choice in routing_summary (e.g., 'tissue_filtered' or 'specialization_filtered') and proceed with the existing single-primary (and at most one deterministic secondary) call and strict post-return intersection grounding. This rule is additive to and must be applied before the existing tie-breaks; any pre-call or runtime mismatch remains a fatal routing violation and must produce the standardized per-profile abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810488+00:00",
      "updated_at": "2026-06-01T21:54:33.810491+00:00"
    },
    "expert_routing-00033": {
      "id": "expert_routing-00033",
      "section": "expert_routing",
      "content": "Mandatory-primary-call-and-no-internal-heuristics: For any profile with non-empty profile.candidate_experts the orchestrator MUST atomically: (A) compute per-candidate intersections and earliest_index, build a routing_summary (including chosen_primary_expert_name) and include that routing_summary VERBATIM in a single-profile payload that also contains profile_id, question_text, supplied_ordered_genes, task_family/task_type, grounded_context, and ordered candidate_labels; (B) invoke routing_summary.chosen_primary_expert_name as the first runtime call with that single-profile payload; and (C) wait for and ground the returned expert payload before emitting any per-profile final fields. The orchestrator MUST NOT emit any final_label, alternative_labels, predicted_tissue, supporting_genes, negative_markers, rationale, expert_models_used, confidence, or abstain for that profile prior to receiving and grounding the called expert's return. Any attempt to produce a final_label or to synthesize evidence using internal heuristics, domain rules, or other sources without a valid called expert is a fatal routing violation for that profile and MUST produce the standardized per-profile abstain JSON exactly: final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true, with a concise rationale naming the violation (e.g., \"internal-heuristic labeling without expert call\").",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810498+00:00",
      "updated_at": "2026-06-01T21:54:33.810501+00:00"
    },
    "expert_routing-00034": {
      "id": "expert_routing-00034",
      "section": "expert_routing",
      "content": "Atomic per-profile deterministic routing_and_grounded_acceptance: For every profile do one atomic sequence over ONLY profile.candidate_experts: (1) Pre-call compute for each candidate_expert intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). Deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match to profile.tissue → tie-break profile.candidate_experts list order. Build a routing_summary listing each candidate's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include this routing_summary VERBATIM in the single-profile tool payload (payload must also contain profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify BEFORE any call that payload.candidate_experts == profile.candidate_experts, payload.routing_summary == the computed routing_summary, and intended expert_name == routing_summary.chosen_primary_expert_name (which must be a member of profile.candidate_experts). If any pre-call check fails, DO NOT call any expert and emit the standardized per-profile abstain output exactly. (2) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; do NOT call non-candidate experts under any circumstance. (3) Post-return grounding: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and is in profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; record expert_models_used = the exact runtime-ordered list of called experts for that profile. (4) Acceptance rule: accept (abstain=false) the primary ONLY if supporting_genes non-empty AND (A) supporting_genes contains ≥1 returned.canonical_routing_marker OR (B) returned.final_label unambiguously maps to one provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (apply the same tie-breaks), sending a single-profile payload that includes that secondary-call routing_summary verbatim; after the secondary returns compute its intersections, append it to expert_models_used, and apply the same acceptance rules. If neither called expert is accepted, emit the standardized per-profile abstain output exactly. (5) Synthesis grounding: when accepting an expert, populate final fields strictly from the accepted expert and the computed intersections only: final_label = accepted_expert.final_label mapped to one of the provided candidate_labels; alternative_labels = ordered candidate_labels; predicted_tissue = accepted_expert.canonical_tissue verbatim; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include the field even if empty); rationale must cite only these computed intersections; expert_models_used must equal the runtime-ordered invoked experts; confidence set deterministically: 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. Any deviation from this atomic contract (missing/altered routing_summary, payload.candidate_experts mismatch, first-called != chosen_primary_expert_name, invocation of non-candidate expert, evidence not strictly subsetted from the computed intersections, mixing evidence across profiles, omission of negative_markers, or expert_models_used mismatch with runtime trace) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain output exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810509+00:00",
      "updated_at": "2026-06-01T21:54:33.810513+00:00"
    },
    "expert_output_synthesis-000XX": {
      "id": "expert_output_synthesis-000XX",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-000XX (atomic per-profile routing→call→synthesis contract): For every profile perform one atomic sequence over ONLY profile.candidate_experts: (A) Pre-call: compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) and earliest_index; deterministically choose chosen_primary_expert by the existing tie-breaks; build a routing_summary listing each candidate's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include that routing_summary VERBATIM in the single-profile payload and verify payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name (member of profile.candidate_experts). If any pre-call check fails, DO NOT call any expert and emit the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (B) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never call non-candidate experts. (C) Post-return grounding: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; set expert_models_used = the exact runtime-ordered list of called experts for that profile. (D) Acceptance/secondary: accept (abstain=false) the primary ONLY if supporting_genes non-empty AND (1) supporting_genes contains >=1 returned.canonical_routing_marker OR (2) returned.final_label unambiguously maps to one provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (apply same tie-breaks), send a single-profile payload that includes that secondary-call routing_summary verbatim, append secondary to expert_models_used, compute intersections for the secondary, and apply the same grounding and acceptance rules. Do NOT call more than two experts. (E) Synthesis grounding: when accepting an expert, populate final fields strictly from the accepted expert and the computed intersections only: final_label = accepted_expert.final_label mapped to one of the provided candidate_labels; alternative_labels = ordered candidate_labels; predicted_tissue = accepted_expert.canonical_tissue verbatim; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); rationale must cite only these computed intersections; expert_models_used must equal the runtime-ordered invoked experts; confidence deterministically: 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains >=1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (F) Fatal violations & abstain: any deviation from this atomic contract (missing/altered routing_summary, payload.candidate_experts mismatch, first-called/returned expert != chosen_primary_expert_name, invocation of non-candidate expert, using evidence not strictly subsetted from the accepted expert's returned intersections, evidence mixed across profiles, omitted negative_markers field, or final expert_models_used mismatch with runtime trace) MUST produce the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) and include a concise rationale naming the violated check. Never invent markers/tissues or mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810527+00:00",
      "updated_at": "2026-06-01T21:54:33.810531+00:00"
    },
    "expert_routing-NEW01": {
      "id": "expert_routing-NEW01",
      "section": "expert_routing",
      "content": "Enhance expert_routing-NEW01 with an explicit non-zero-intersection exclusion: when computing per-candidate intersection_count and deterministically choosing chosen_primary_expert, if ANY profile.candidate_expert has intersection_count > 0 then the chosen_primary_expert MUST be selected from the subset of candidates with intersection_count > 0. Only if all candidates have intersection_count == 0 may a zero-intersection candidate be chosen (apply existing fallback-call rule). Record which filter was applied in routing_summary. Enforce the existing atomic pre-call/payload checks and that the runtime first-called expert_name exactly equals routing_summary.chosen_primary_expert_name; any deviation is a fatal routing violation for that profile and MUST produce the standardized per-profile abstain output. ",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810538+00:00",
      "updated_at": "2026-06-01T21:54:33.810542+00:00"
    },
    "expert_routing-NEW02": {
      "id": "expert_routing-NEW02",
      "section": "expert_routing",
      "content": "expert_routing-NEW02: Atomic per-profile routing→payload→call→synthesis contract (single-rule summary) — For every profile perform one atomic sequence over ONLY profile.candidate_experts: (A) Pre-call: compute for each candidate_expert intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null); deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match to profile.tissue → tie-break candidate_experts list order. Build a routing_summary listing each candidate_expert's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include that routing_summary VERBATIM in the single-profile tool payload (payload must also contain profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify pre-call that payload.candidate_experts == profile.candidate_experts, payload.routing_summary == computed routing_summary, and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; if any pre-call check fails, DO NOT call any expert and emit the standardized per-profile abstain output exactly. (B) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. (C) Post-return grounding: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and is in profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; set expert_models_used = the exact runtime-ordered list of called experts for that profile. (D) Acceptance/mapping/secondary: accept (abstain=false) the primary ONLY if supporting_genes non-empty AND (1) supporting_genes contains ≥1 returned.canonical_routing_marker OR (2) returned.final_label unambiguously maps to one provided candidate_label. final_label MUST be the exact provided candidate_label that corresponds to the accepted_expert.final_label (no freeform relabeling). If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim) and repeat (C)-(D) once; do not call more than two experts. (E) Final grounding & confidence: when accepting an expert populate final fields strictly from that accepted expert and computed intersections only: final_label = accepted_expert.final_label mapped exactly to one provided candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue verbatim; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); rationale must cite only these computed intersections; expert_models_used must equal the runtime-ordered invoked experts; set confidence deterministically: 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (F) Fatal deviations & abstain: any deviation from this atomic contract (missing/altered routing_summary in payload, payload.candidate_experts != profile.candidate_experts, first-called/returned expert != routing_summary.chosen_primary_expert_name, invocation of any non-candidate expert, final_label not mapping to a provided candidate_label, supporting_genes/negative_markers/predicted_tissue not strict intersections with supplied_ordered_genes and the accepted expert's returned fields, expert_models_used not equal to the runtime-ordered call trace, evidence mixed across profiles, or calling >2 experts) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain output exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check and offending expert(s).",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810549+00:00",
      "updated_at": "2026-06-01T21:54:33.810552+00:00"
    },
    "expert_output_synthesis-00035": {
      "id": "expert_output_synthesis-00035",
      "section": "expert_output_synthesis",
      "content": "Augment expert_output_synthesis-00035 with an explicit atomic-post-return enforcement and single-secondary mandate: After invoking the routing_summary.chosen_primary_expert_name and receiving its return, immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that it is a member of profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). If supporting_genes is empty AND profile.candidate_experts.size>1 the orchestrator MUST perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send that secondary's routing_summary verbatim), verify the secondary's returned.expert_name matches its routing_summary.chosen_primary_expert_name, compute its intersections, and accept only if the secondary passes the same mapping+non-empty-supporting_genes checks. When accepting an expert (primary or secondary) populate every per-profile final field strictly from that accepted expert's returned fields intersected with supplied_ordered_genes only: final_label = accepted_expert.final_label mapped exactly to one of the provided candidate_labels; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); expert_models_used = exact runtime-ordered list of invoked experts for that profile; rationale must cite only the computed supporting_genes and negative_markers intersections; confidence set deterministically per existing rules. Any deviation from these steps (including inventing or adding genes, mixing evidence across experts/profiles, omitting negative_markers, skipping the single deterministic secondary when required, or final_label not mapping to a provided candidate_label) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain JSON exactly as specified elsewhere.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810559+00:00",
      "updated_at": "2026-06-01T21:54:33.810563+00:00"
    },
    "evidence_grounding-000YY": {
      "id": "evidence_grounding-000YY",
      "section": "evidence_grounding",
      "content": "Tighten atomic acceptance & single-secondary fallback: Acceptance of a primary (or permitted secondary) expert is allowed ONLY when BOTH (1) the returned.final_label unambiguously maps exactly to one of the provided profile.candidate_labels, AND (2) supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) is non-empty. Compute negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) and include negative_markers (may be empty) in every per-profile final output. If the primary fails either the mapping check OR supporting_genes is empty AND profile.candidate_experts.size>1, perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim) and apply these identical atomic checks to the secondary. If no permitted secondary exists or the secondary also fails mapping or yields empty supporting_genes, emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call per-candidate intersections if available else [], negative_markers = computed intersections if available else [], expert_models_used = runtime-ordered called experts (or [] if no valid call), confidence = \"low\", abstain = true) and include a concise rationale naming whether mapping failed and/or supporting_genes was empty. When accepting, derive all final fields strictly from the accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapped provided candidate_label, alternative_labels = ordered profile.candidate_labels, predicted_tissue = accepted_expert.canonical_tissue exactly, supporting_genes = computed intersection, negative_markers = computed intersection (include even if empty), expert_models_used = exact runtime-ordered invoked experts, rationale must cite only the computed intersections, and confidence set deterministically per existing rules. Any deviation from these mapping+intersection+single-secondary requirements is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain JSON exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810570+00:00",
      "updated_at": "2026-06-01T21:54:33.810574+00:00"
    },
    "expert_output_synthesis-NEW01": {
      "id": "expert_output_synthesis-NEW01",
      "section": "expert_output_synthesis",
      "content": "Atomic per-profile contract: For every profile perform one atomic routing_summary → first-call → post-return → synthesis sequence restricted to ONLY profile.candidate_experts. Pre-call: compute a routing_summary over profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify BEFORE calling that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. Post-return verification: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name (or equals the single permitted deterministic secondary when a secondary was invoked) AND returned.expert_name ∈ profile.candidate_experts. Compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; record expert_models_used = exact runtime-ordered invoked experts for that profile. Acceptance: accept (abstain=false) ONLY when final_label unambiguously maps to one provided profile.candidate_label AND supporting_genes is non-empty (and, when required, contains the canonical routing marker overlap or includes all user_provided_negative_markers). Single-secondary fallback: if primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (include that secondary-call routing_summary verbatim) and apply the identical post-return checks to the secondary only. Synthesis grounding: when accepting, populate every per-profile final field STRICTLY from the accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels (final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; supporting_genes and negative_markers = computed intersections; predicted_tissue = accepted_expert.canonical_tissue exactly; rationale cites only those computed intersections; expert_models_used = runtime-ordered invoked experts; confidence = deterministic mapping per existing rules). Fatal deviations & standardized per-profile abstain: any pre-call or runtime contract failure (missing/altered routing_summary in payload, payload.candidate_experts != profile.candidate_experts, first-called/returned expert != routing_summary.chosen_primary_expert_name, any invoked expert not in profile.candidate_experts, returned.final_label not mapping to candidate_labels, supporting_genes/negative_markers/predicted_tissue not strict intersections of the accepted expert's returned fields with supplied_ordered_genes, evidence mixed across profiles, expert_models_used mismatch with the runtime call trace, calling >2 experts, or invention of markers/tissues) MUST immediately produce the standardized per-profile abstain JSON EXACTLY with these fields: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain.rationale MUST be a single concise sentence naming the specific violation and list offending expert_name(s) when applicable. Never accept or synthesize outputs from non-candidate experts, never mix evidence across profiles, and never emit non-abstain final fields prior to a validated expert return.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810581+00:00",
      "updated_at": "2026-06-01T21:54:33.810585+00:00"
    },
    "expert_routing-00036": {
      "id": "expert_routing-00036",
      "section": "expert_routing",
      "content": "Atomic deterministic candidate-only routing_summary and first-call enforcement — For every profile with non-empty profile.candidate_experts compute for ONLY those candidate_experts: intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). Deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact tissue match to profile.tissue → tie-break profile.candidate_experts list order. Build a routing_summary listing each candidate_expert's intersection_count and earliest_index and include chosen_primary_expert_name and that chosen expert's metrics; include this routing_summary VERBATIM in the single-profile outgoing tool payload (payload must also contain profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify BEFORE any call that payload.candidate_experts == profile.candidate_experts, payload.routing_summary == the computed routing_summary, and intended expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. If at runtime any invoked expert is not a member of profile.candidate_experts OR the first-called/returned expert_name != routing_summary.chosen_primary_expert_name, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from any returned outputs for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation and offending expert(s) (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\").",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810592+00:00",
      "updated_at": "2026-06-01T21:54:33.810596+00:00"
    },
    "conflict_and_abstain-00037": {
      "id": "conflict_and_abstain-00037",
      "section": "conflict_and_abstain",
      "content": "Tighten atomic pre-call gating, retroactive-fatal enforcement, and mandatory standardized per-profile abstain replacement: Before invoking any expert for a profile the orchestrator MUST atomically compute the per-profile routing_summary over ONLY profile.candidate_experts and include that routing_summary VERBATIM in the single-profile payload. Atomically verify pre-call that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. Rationale must concisely name the specific failure (e.g., \"payload.routing_summary mismatch\"). Retroactive-fatal clause: if at runtime any expert call is observed for a profile despite any pre-call check failing OR if the runtime-first-called expert_name != routing_summary.chosen_primary_expert_name OR any invoked expert is not a member of profile.candidate_experts, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: (A) discard and DO NOT accept, synthesize from, or reference any returned expert outputs for that profile (ignore returned supporting_genes/negative_markers/labels/tissue/confidence), (B) stop any further expert calls for that profile, (C) compute supporting_genes = the pre-call per-profile intersections if available else [], and (D) IMMEDIATELY replace/emit the standardized per-profile abstain JSON EXACTLY with fields as above and a concise rationale naming the violation and offending expert(s) (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Additionally, before accepting any non-abstain per-profile final output verify final_answer.expert_models_used exactly equals the runtime-ordered list of invoked experts for that profile; any mismatch is a fatal violation and MUST be handled as above (replace with the standardized per-profile abstain JSON). Do not attempt recovery, do not mix evidence across profiles, and do not synthesize from unauthorized expert outputs under any circumstances.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810603+00:00",
      "updated_at": "2026-06-01T21:54:33.810607+00:00"
    },
    "expert_output_synthesis-00038": {
      "id": "expert_output_synthesis-00038",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-ENFORCE_ROUTING_AND_SYNTHESIS_CHECKLIST: Atomic per-profile routing→payload→call→verification→synthesis checklist (mandatory). For every profile do this single atomic sequence over ONLY profile.candidate_experts: (1) Pre-call routing_summary: compute for each candidate_expert intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null); deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate_experts list order. Build routing_summary listing each candidate's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics. Insert this routing_summary VERBATIM into the single-profile tool payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify BEFORE call that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary; if this verification fails, DO NOT call any expert and immediately emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (2) Call-order: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile and record expert_models_used in runtime order. If the runtime first-called expert is not routing_summary.chosen_primary_expert_name or is not a member of profile.candidate_experts treat as a fatal routing violation and emit the standardized per-profile abstain JSON exactly (use pre-call intersections for supporting_genes if available) with a concise rationale naming the offending expert. (3) Post-return verification & intersections: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that it is a member of profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly. Do NOT add, invent, or mix genes/markers/tissue/labels from other experts or profiles. (4) Acceptance & single deterministic secondary: accept (abstain=false) the primary ONLY if supporting_genes non-empty AND (A) supporting_genes contains ≥1 returned.canonical_routing_marker OR (B) returned.final_label unambiguously maps to one provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (build/send a single-profile payload that includes that secondary-call routing_summary verbatim), append secondary to expert_models_used in runtime order, repeat step (3) for the secondary, and apply the same acceptance checks. Do not call more than two experts. (5) Final-field atomic derivation on accept: when an expert is accepted populate final fields STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes only: final_label = mapping(accepted_expert.final_label → one provided candidate_label) (must match provided candidate_labels), alternative_labels = ordered profile.candidate_labels, predicted_tissue = accepted_expert.canonical_tissue exactly, supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes), negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty), rationale = concise statement citing ONLY these computed supporting_genes and negative_markers intersections, expert_models_used = exact runtime-ordered list of called experts for that profile, and abstain=false. (6) Deterministic confidence mapping: set confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (7) Fatal deviations, conflicts, OOD, and abstain: any deviation from this atomic checklist (missing/altered routing_summary, payload.candidate_experts mismatch, first-called != chosen_primary_expert_name, invocation of non-candidate expert, evidence not strictly subsetted from the accepted expert's returned intersections, final_label not mapping to provided candidate_labels, evidence mixed across profiles, omission of negative_markers or expert_models_used, or calling >2 experts) MUST produce the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check or offending expert. This single atomic checklist enforces deterministic routing, mandatory per-profile payloads, strict post-return grounding, single-secondary fallback, required fields, and standardized abstain/conflict handling.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810613+00:00",
      "updated_at": "2026-06-01T21:54:33.810617+00:00"
    },
    "expert_routing-NEW03": {
      "id": "expert_routing-NEW03",
      "section": "expert_routing",
      "content": "Atomic candidate-only routing→payload→call→synthesis contract: For every profile perform one atomic sequence over ONLY profile.candidate_experts: (1) Pre-call compute per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index; deterministically choose routing_summary.chosen_primary_expert by existing tie-breaks (highest intersection_count → smallest earliest_index → profile.tissue match → candidate order). Build routing_summary listing each candidate's intersection_count and earliest_index and include chosen_primary_expert_name and that chosen expert's metrics; include this routing_summary VERBATIM in the single-profile outgoing tool payload (payload must also contain profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify BEFORE any call that payload.candidate_experts == profile.candidate_experts, payload.routing_summary == computed routing_summary, profile.candidate_experts non-empty, and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any expert and emit the standardized per-profile abstain JSON exactly. (2) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile and record runtime-ordered expert_models_used. Never call non-candidate experts. (3) Post-return grounding: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and is in profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly. (4) Acceptance & deterministic-secondary: accept the primary (abstain=false) ONLY if supporting_genes non-empty AND (A) supporting_genes contains ≥1 returned.canonical_routing_marker OR (B) returned.final_label unambiguously maps to one provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim), append it to expert_models_used, compute intersections for the secondary, and apply the same checks; do not call more than two experts. (5) Final-field grounding on accept: when an expert is accepted, populate final fields STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes only: final_label = mapping(accepted_expert.final_label → one provided candidate_label) (must match provided candidate_labels), alternative_labels = ordered profile.candidate_labels, predicted_tissue = accepted_expert.canonical_tissue exactly, supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes), negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include the field even if empty), rationale = concise statement citing ONLY these computed supporting_genes and negative_markers intersections, expert_models_used = exact runtime-ordered list of invoked experts for that profile, and abstain=false. (6) Deterministic confidence mapping: set confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (7) Fatal deviations & standardized abstain: any deviation from this atomic contract (missing/altered routing_summary in payload, payload.candidate_experts != profile.candidate_experts, first-called/returned expert != routing_summary.chosen_primary_expert_name, invocation of any non-candidate expert, final_label not mapping to a provided candidate_label, supporting_genes/negative_markers/predicted_tissue not strict intersections of the accepted expert's returned fields with supplied_ordered_genes, omission of negative_markers, expert_models_used mismatch with runtime trace, evidence mixed across profiles, or calling >2 experts) MUST produce the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check and offending expert(s). This single atomic rule enforces deterministic candidate-only routing, mandatory routing_summary payloads, strict intersection grounding for every final field (including negative_markers), exact expert_models_used tracing, single-secondary fallback, deterministic confidence, and immediate standardized per-profile abstain on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810624+00:00",
      "updated_at": "2026-06-01T21:54:33.810628+00:00"
    },
    "expert_output_synthesis-NEW03": {
      "id": "expert_output_synthesis-NEW03",
      "section": "expert_output_synthesis",
      "content": "Augment expert_output_synthesis-NEW03 with mandatory atomic pre-call verification and fatal-runtime non-candidate handling: Before invoking any expert for a profile, atomically compute the per-profile routing_summary over ONLY profile.candidate_experts and verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call. At runtime immediately verify the first-returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert ∈ profile.candidate_experts. If ANY pre-call or runtime check fails (including any invoked non-candidate expert or first-called != chosen_primary), TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: (A) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (B) Stop any further expert calls for that profile; (C) Compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if those pre-call intersections were computed and available, otherwise supporting_genes = []; (D) Immediately emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = the ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (E) Set the abstain.rationale to a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (example: \"payload.routing_summary mismatch; attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Atomically replace any previously-emitted per-profile fields with this exact standardized abstain JSON before returning the overall response. Do not mix evidence across profiles, do not accept or surface any data returned from unauthorized experts for that profile, and do not perform any recovery attempts for the affected profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810634+00:00",
      "updated_at": "2026-06-01T21:54:33.810638+00:00"
    },
    "tool_input_restructuring-00039": {
      "id": "tool_input_restructuring-00039",
      "section": "tool_input_restructuring",
      "content": "Per-profile routing_summary equality gate & retroactive standardized-abstain: For every profile compute a routing_summary (per-candidate intersection_count and earliest_index plus chosen_primary_expert_name) and include that routing_summary verbatim in the single-profile outgoing payload before any expert call. Atomically verify pre-call that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary exactly equals the orchestrator-computed routing_summary. If this verification fails, DO NOT call any experts for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON exactly with these fields: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain rationale must be a concise sentence naming the failure (e.g., \"missing or altered routing_summary in payload\"). If at runtime an expert call is observed despite the routing_summary mismatch OR any invoked expert is not a member of profile.candidate_experts, TREAT THIS as a retroactive fatal routing violation: do NOT accept or synthesize from any returned outputs for that profile; stop further calls for that profile; compute supporting_genes = pre-call intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (same fields as above) with a concise rationale that lists the contractual violation and the offending expert_name(s) (e.g., \"routing_summary missing; attempted call to non-candidate expert 'X'\"). Do not mix evidence across profiles when producing these abstains.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810644+00:00",
      "updated_at": "2026-06-01T21:54:33.810648+00:00"
    },
    "expert_routing-00040": {
      "id": "expert_routing-00040",
      "section": "expert_routing",
      "content": "Atomic per-profile candidate-only call + acceptance contract: For each profile do one atomic routing→payload→call→post-return→synthesis sequence restricted to profile.candidate_experts: (A) Pre-call: compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) and earliest_index; deterministically choose chosen_primary_expert (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate_experts order). Insert this routing_summary VERBATIM in the single-profile payload. Verify pre-call that profile.candidate_experts non-empty and that the intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any expert and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (B) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke any expert not in profile.candidate_experts. If a non-candidate is invoked, treat as fatal and emit the standardized per-profile abstain JSON as above naming the offending expert. (C) Post-return grounding: immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts. Compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Always include negative_markers (may be empty) in the per-profile final output. (D) Acceptance rule: accept (abstain=false) the primary ONLY when BOTH (1) returned.final_label unambiguously maps exactly to one of the provided profile.candidate_labels AND (2) supporting_genes is non-empty. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send that secondary's routing_summary verbatim) and repeat steps (B)-(D) once; do not call more than two experts. (E) On accept: populate final fields strictly from the accepted expert's returned fields intersected with supplied_ordered_genes only: final_label = mapped provided candidate_label, alternative_labels = ordered profile.candidate_labels, predicted_tissue = accepted_expert.canonical_tissue exactly, supporting_genes = computed intersection, negative_markers = computed intersection, expert_models_used = exact runtime-ordered list of called experts for that profile, rationale = concise citation of only the computed supporting_genes and negative_markers intersections. (F) Confidence mapping: 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains >=1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (G) Failure/abstain: if no accepted expert after allowed calls OR any pre-call/post-call contract violation occurs (including payload/routing_summary mismatch, first-called != chosen_primary, invocation of non-candidate expert, final_label not mapping, evidence not strict intersections, missing negative_markers, or evidence-mixing across profiles) emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call intersections if available else [], negative_markers = [], expert_models_used = [] (or the exact runtime-ordered called experts if valid calls occurred), confidence = \"low\", abstain = true) with a concise rationale naming the specific violation or offending expert. This rule forbids calling non-candidate experts, mixing evidence across profiles, inventing markers/tissues/labels, and enforces the single-primary + at most one deterministic secondary acceptance workflow.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810655+00:00",
      "updated_at": "2026-06-01T21:54:33.810659+00:00"
    },
    "evidence_grounding-00077": {
      "id": "evidence_grounding-00077",
      "section": "evidence_grounding",
      "content": "evidence_grounding-00077: Required-exclusion-marker check & deterministic-secondary-or-abstain — When the incoming query includes a non-empty user_provided_negative_markers list (i.e., explicit exclusion markers to rule out alternatives), enforce this atomic post-return contract for each profile after any allowed primary (and at most one permitted secondary) expert call: (1) Compute negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes, user_provided_negative_markers) as usual. (2) If negative_markers does NOT contain every user_provided_negative_marker (i.e., one or more user-specified exclusion markers are absent from the computed intersection), TREAT THE PRIMARY AS FAILED FOR ACCEPTANCE due to missing required exclusions. Do NOT accept the primary final_label on that basis alone. (3) If a permitted deterministic secondary call remains (primary called, profile.candidate_experts.size>1, and secondary not yet invoked), perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim), compute that secondary's intersections (including negative_markers intersection with user_provided_negative_markers), and apply all normal acceptance/grounding checks to the secondary. (4) If no permitted secondary exists OR the secondary also fails to include all user_provided_negative_markers in its computed negative_markers intersection, IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections if available else [], negative_markers = [] or the computed intersection if present, expert_models_used = runtime-ordered called experts (or [] if no valid call), confidence = \"low\", abstain = true). The abstain rationale MUST concisely list which user_provided_negative_markers were expected but absent (e.g., \"missing expected negative markers: IFNG, GZMB\") and name that either the secondary was attempted and failed or that no secondary was available. (5) Do NOT invent, substitute, or add negative markers; draw all negative_markers only from the computed intersections. This rule is atomic and additive to existing intersection/mapping/secondary/abstain contracts and enforces deterministic handling when user-specified exclusion markers are required to rule out alternatives.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810665+00:00",
      "updated_at": "2026-06-01T21:54:33.810669+00:00"
    },
    "expert_routing-00041": {
      "id": "expert_routing-00041",
      "section": "expert_routing",
      "content": "Atomic per-profile routing_summary gate & fatal non-candidate-call rule: For each profile compute per-candidate intersections = intersection(expert.canonical_routing_markers, supplied_ordered_genes) and earliest_index over ONLY profile.candidate_experts, deterministically choose chosen_primary_expert (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate order), and build a routing_summary listing each candidate's intersection_count and earliest_index plus chosen_primary_expert_name. Insert this routing_summary VERBATIM in the single-profile tool payload and atomically verify pre-call that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. If at any time the runtime-invoked expert is NOT a member of profile.candidate_experts OR the first-called expert != routing_summary.chosen_primary_expert_name, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned outputs for that profile; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true, and a concise rationale naming the violation (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). For allowed returns verify returned.expert_name == chosen_primary_expert_name and compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) and set predicted_tissue = returned.canonical_tissue exactly and expert_models_used = [first_expert_name]. Accept primary (abstain=false) ONLY if supporting_genes non-empty AND (supporting_genes contains ≥1 returned.canonical_routing_marker OR returned.final_label unambiguously maps to a provided candidate_label). If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim), append secondary to expert_models_used, repeat these verifications and intersection computations once, and accept only if the secondary passes the same checks. If no permitted accepted expert after allowed calls, IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (use post-call intersections when valid expert returns exist, else fall back to pre-call intersections), include expert_models_used = runtime-ordered called experts (or [] if no valid calls), set confidence = \"low\", abstain = true, and include a concise rationale naming whether mapping failed or supporting_genes was empty. Never call non-candidate experts, never mix evidence across profiles, never invent or add supporting_genes/negative_markers/tissues/labels, and always record expert_models_used exactly in runtime order.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810676+00:00",
      "updated_at": "2026-06-01T21:54:33.810680+00:00"
    },
    "expert_routing-REFL01": {
      "id": "expert_routing-REFL01",
      "section": "expert_routing",
      "content": "expert_routing-REFL01: Atomic per-profile deterministic routing→payload→call→grounded-synthesis contract — For every profile run one atomic sequence over ONLY profile.candidate_experts: (1) Pre-call routing computation: for each candidate_expert compute intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest index of any matched canonical marker (or null). If any candidate has intersection_count>0 restrict chosen_primary_expert to that subset; deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break profile.candidate_experts list order. Build a routing_summary that lists each candidate_expert's intersection_count and earliest_index and includes chosen_primary_expert_name and its metrics. (2) Single-profile payload & pre-call gates: include this routing_summary VERBATIM in the single-profile tool payload (payload must also contain profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify BEFORE calling: payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (3) First-call fidelity: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. If at runtime any invoked expert is not in profile.candidate_experts OR the first-called/returned expert_name != routing_summary.chosen_primary_expert_name, TREAT AS A FATAL ROUTING VIOLATION for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (use pre-call intersections when available) with a concise rationale naming the offending expert. (4) Immediate post-return grounding: upon expert return verify returned.expert_name == routing_summary.chosen_primary_expert_name and is in profile.candidate_experts. Compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Set predicted_tissue = returned.canonical_tissue exactly and set expert_models_used = [first_expert_name] at this point. Do NOT add, invent, or mix genes/markers/tissue/labels from other experts or profiles. (5) Acceptance gating (primary): accept (abstain=false) the primary ONLY if supporting_genes non-empty AND (A) supporting_genes contains ≥1 returned.canonical_routing_marker OR (B) returned.final_label unambiguously maps exactly to one provided profile.candidate_label. (6) Deterministic single-secondary fallback: if the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (choose by next-largest intersection_count using same tie-breaks). For the secondary call build/send a single-profile payload that includes its routing_summary verbatim, atomically verify payload equality before calling, invoke the secondary, verify returned.expert_name == its routing_summary.chosen_primary_expert_name and is in profile.candidate_experts, compute intersections for the secondary, append secondary to expert_models_used in runtime order, and apply the identical acceptance checks. Do NOT call more than two experts. (7) Final-field atomic derivation on accept: when an expert (primary or accepted secondary) is accepted, populate final fields STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes: final_label = mapping(accepted_expert.final_label → one provided candidate_label) (must match provided candidate_labels), alternative_labels = ordered profile.candidate_labels, predicted_tissue = accepted_expert.canonical_tissue exactly, supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes), negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty), rationale = concise citation limited to the computed supporting_genes and negative_markers intersections, expert_models_used = exact runtime-ordered invoked experts for that profile, abstain=false. (8) Deterministic confidence mapping: set confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (9) Fatal deviations → standardized per-profile abstain: any deviation from these atomic obligations (missing/altered routing_summary in payload, payload.candidate_experts != profile.candidate_experts, first-called/returned expert != routing_summary.chosen_primary_expert_name, calling any non-candidate expert, using evidence not strictly subsetted from the accepted expert's returned intersections, mixing evidence across profiles, final_label not mapping to provided candidate_labels, omission of negative_markers, expert_models_used mismatch with runtime trace, or calling >2 experts) MUST produce the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check or offending expert(s).",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810687+00:00",
      "updated_at": "2026-06-01T21:54:33.810691+00:00"
    },
    "tool_input_restructuring-NEW-FIRST_CALL_FIDELITY": {
      "id": "tool_input_restructuring-NEW-FIRST_CALL_FIDELITY",
      "section": "tool_input_restructuring",
      "content": "Strengthen atomic first-call-fidelity: For every single-profile outgoing payload compute the routing_summary over ONLY profile.candidate_experts and include it VERBATIM in the payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == the orchestrator-computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert is a member of profile.candidate_experts. If ANY of these checks fails (routing_summary missing/altered, payload.candidate_experts mismatch, intended/first-called expert != chosen_primary_expert_name, or any invoked expert ∉ profile.candidate_experts), TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: (1) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (2) Stop any further expert calls for that profile; (3) Compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if available else []; (4) Emit the standardized per-profile abstain JSON EXACTLY with fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (5) The abstain rationale MUST be a concise sentence naming the specific violation and listing the offending expert_name(s) when applicable (e.g., \"attempted call to non-candidate expert 'tabula_sapiens_ovary_cell_annotation'\" or \"first-called 'X' != chosen_primary 'Y'\"). Do not attempt recovery or mix evidence from other profiles when producing this abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810697+00:00",
      "updated_at": "2026-06-01T21:54:33.810701+00:00"
    },
    "expert_routing-00042": {
      "id": "expert_routing-00042",
      "section": "expert_routing",
      "content": "Mandatory per-profile expert-invocation & atomic-grounding: For every profile with non-empty profile.candidate_experts the orchestrator MUST (A) invoke exactly the chosen_primary_expert (computed deterministically from profile.candidate_experts and supplied_ordered_genes) as the first call for that profile and may perform at most one deterministic secondary call only if the primary fails acceptance; (B) record the runtime call trace and include that exact ordered list in final_answer.expert_models_used when accepting (abstain=false); (C) if the runtime call trace shows ZERO invoked experts for a profile that had non-empty candidate_experts OR any invoked expert is not a member of profile.candidate_experts, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from any expert-free/internal heuristics, stop further calls for that profile, compute supporting_genes = pre-call per-profile intersections if available else [], and immediately emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections or [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation (e.g., \"no expert called; internal labeling attempted\" or \"attempted call to non-candidate expert 'X'\"). When accepting, derive every final field strictly from the accepted expert's returned fields intersected with the supplied_ordered_genes and map the accepted_expert.final_label to one of the provided candidate_labels; do NOT invent markers/tissues or mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810707+00:00",
      "updated_at": "2026-06-01T21:54:33.810711+00:00"
    },
    "expert_output_synthesis-00043": {
      "id": "expert_output_synthesis-00043",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-NEW04: Consolidated atomic per-profile routing→payload→call→synthesis contract — For every profile perform one atomic sequence over ONLY profile.candidate_experts: (1) Pre-call: compute a routing_summary listing each candidate_expert's intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index (smallest matched index or null) and deterministically choose chosen_primary_expert (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate_experts list order). If any candidate has intersection_count>0 the chosen_primary_expert MUST be selected from that subset; record applied filters in routing_summary. (2) Payload & pre-call gate: include this routing_summary VERBATIM in the single-profile tool payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels) and atomically verify BEFORE calling that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, do NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (3) Call-order: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. (4) Immediate post-return verification: upon expert return verify returned.expert_name == routing_summary.chosen_primary_expert_name and that it is a member of profile.candidate_experts. If this verification fails (including any invoked expert not in profile.candidate_experts), TREAT AS A FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from any returned outputs for that profile; compute supporting_genes = pre-call intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (as above) with a concise rationale naming the offending expert. (5) Strict intersections: compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). Do NOT add, invent, or substitute any genes, markers, tissues, or labels. Always include negative_markers (may be empty). (6) Acceptance gating (primary): accept (abstain=false) the primary ONLY if BOTH (A) supporting_genes is non-empty AND (B) returned.final_label unambiguously maps exactly to one of the provided profile.candidate_labels. final_label in the output MUST be that provided candidate_label (no freeform relabeling). (7) Deterministic single-secondary fallback: if the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (build/send a new single-profile payload that includes that secondary-call routing_summary verbatim), append the secondary to expert_models_used in runtime order, verify its returned.expert_name matches its routing_summary.chosen_primary_expert_name and is a member of profile.candidate_experts, compute its intersections as above, and apply the identical acceptance checks. Do NOT call more than two experts. (8) Final-field atomic derivation on accept: when an expert (primary or accepted secondary) is accepted populate every final field STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapping(accepted_expert.final_label → one provided candidate_label) (must match provided candidate_labels); alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); expert_models_used = exact runtime-ordered list of invoked experts for that profile; rationale = concise citation limited to the computed supporting_genes and negative_markers intersections only; abstain=false. (9) Deterministic confidence mapping: set confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (10) Fatal deviations & standardized per-profile abstain: any deviation from this atomic contract (missing/altered routing_summary in payload, payload.candidate_experts != profile.candidate_experts, first-called/returned expert != routing_summary.chosen_primary_expert_name, invocation of any non-candidate expert, final_label not mapping to a provided candidate_label, supporting_genes/negative_markers/predicted_tissue not strict intersections of the accepted expert's returned fields with supplied_ordered_genes, missing negative_markers field, expert_models_used not equal to the runtime-ordered call trace, evidence mixed across profiles, or calling >2 experts) MUST produce the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check or offending expert(s). This rule enforces candidate-only deterministic routing, routing_summary verbatim in payloads, single-primary first-call fidelity, a single deterministic secondary fallback when allowed, strict intersection grounding for all emitted fields (including negative_markers), exact expert_models_used tracing, deterministic confidence computation, and immediate per-profile abstain on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810718+00:00",
      "updated_at": "2026-06-01T21:54:33.810722+00:00"
    },
    "evidence_grounding-00044": {
      "id": "evidence_grounding-00044",
      "section": "evidence_grounding",
      "content": "Atomic post-call acceptance & mandatory-negative emission: After the routing_summary.chosen_primary_expert_name is invoked (and at most one deterministic secondary per profile) the orchestrator MUST atomically: (A) verify returned.expert_name == routing_summary.chosen_primary_expert_name and that it is a member of profile.candidate_experts; (B) compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) and include negative_markers (may be empty) in every per-profile final output; (C) accept (abstain=false) the returned.final_label ONLY if BOTH (1) returned.final_label unambiguously maps exactly to one of the provided profile.candidate_labels and (2) supporting_genes is non-empty. If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim) and apply these same atomic checks to the secondary. If no permitted secondary exists or the secondary also fails acceptance, IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections if available else [], negative_markers = [], expert_models_used = the runtime-ordered called experts (or [] if fatal), confidence = \"low\", abstain = true. Rationale in accept/abstain must cite only the computed intersections or name which check failed (e.g., \"supporting_genes empty\" or \"returned.final_label not in candidate_labels\"). Do NOT add, invent, or mix genes/markers/tissues/labels from other experts or domain heuristics. Any mismatch between final_answer.expert_models_used and the runtime-ordered invoked experts is a fatal violation and must produce the standardized per-profile abstain JSON exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810728+00:00",
      "updated_at": "2026-06-01T21:54:33.810733+00:00"
    },
    "expert_output_synthesis-000NEW01": {
      "id": "expert_output_synthesis-000NEW01",
      "section": "expert_output_synthesis",
      "content": "Atomic primary-first-preserve + equality-check → standardized-abstain: For every profile (only over profile.candidate_experts) the orchestrator MUST atomically: (A) compute a per-profile routing_summary (per-candidate intersection_count and earliest_index and deterministically chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). (B) Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. (C) Upon the first-returned expert preserve that expert's returned fields verbatim for grounding and immediately verify these equalities atomically: final.predicted_tissue == first_returned.canonical_tissue (verbatim); final.supporting_genes == intersection(first_returned.supporting_genes, supplied_ordered_genes); final.negative_markers == intersection(first_returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); and final.expert_models_used == the runtime-ordered invoked experts for that profile. (D) If ANY pre-call or post-call equality/check fails (including missing/altered routing_summary in payload; payload.candidate_experts != profile.candidate_experts; first-called != routing_summary.chosen_primary_expert_name; any invoked expert ∉ profile.candidate_experts; predicted_tissue mismatch; supporting_genes/negative_markers not equal to the computed intersections; or expert_models_used mismatch), TREAT THIS AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\"; alternative_labels = the ordered profile.candidate_labels; predicted_tissue = \"\"; supporting_genes = the pre-call per-profile intersections if available else []; negative_markers = []; expert_models_used = []; confidence = \"low\"; abstain = true. The abstain.rationale MUST be a single concise sentence naming the specific equality/contract violation and listing the offending expert_name(s) when applicable (e.g., \"predicted_tissue != first_invoked_expert.canonical_tissue; offending expert 'X'\"). Do NOT accept, synthesize from, or surface any returned expert outputs for that profile when this fatal violation occurs, stop further expert calls for that profile, and do not mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810740+00:00",
      "updated_at": "2026-06-01T21:54:33.810744+00:00"
    },
    "conflict_and_abstain-00002": {
      "id": "conflict_and_abstain-00002",
      "section": "conflict_and_abstain",
      "content": "Atomic per-profile candidate-only routing + primary-first fidelity + single-secondary fallback + strict-intersection synthesis + standardized fatal-abstain: For each profile perform one atomic sequence computed and enforced ONLY over profile.candidate_experts: (A) Pre-call: compute a routing_summary for ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index; deterministically choose chosen_primary_expert_name). Insert that routing_summary VERBATIM into the single-profile outgoing payload (payload must also include profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any expert and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY: final_label = \"\"; alternative_labels = ordered profile.candidate_labels; predicted_tissue = \"\"; supporting_genes = the pre-call per-profile intersections if available else []; negative_markers = []; expert_models_used = []; confidence = \"low\"; abstain = true. The abstain.rationale MUST be a single concise sentence naming the specific contract violation. (B) Call & runtime membership: invoke EXACTLY routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime IMMEDIATELY verify that the first-returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert ∈ profile.candidate_experts. If ANY invoked expert is not a member of profile.candidate_experts OR the first-called != chosen_primary_expert_name OR ZERO experts invoked when candidate_experts non-empty, TREAT THIS AS A FATAL ROUTING VIOLATION: do NOT accept, synthesize from, or reference any returned expert outputs for that profile; stop further expert calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (fields as above). The abstain.rationale MUST be a single concise sentence naming the offending expert_name(s) (e.g., \"attempted call to non-candidate expert 'X'\"). (C) Post-return acceptance & single-secondary fallback: upon a validated returned expert compute computed_supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and computed_negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; record expert_models_used = the exact runtime-ordered invoked experts for that profile. Accept (abstain=false) the primary ONLY when BOTH: (1) returned.final_label unambiguously maps exactly to one provided profile.candidate_label AND (2) computed_supporting_genes is non-empty. If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send that secondary-call routing_summary VERBATIM), append the secondary to expert_models_used in runtime order, compute intersections for the secondary, and apply the identical post-return checks to the secondary only. Do NOT call more than two experts. (D) Final derivation on accept: when an expert (primary or accepted deterministic secondary) is accepted populate every per-profile final field STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = provided candidate_label mapped exactly from accepted_expert.final_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = computed_supporting_genes; negative_markers = computed_negative_markers (include field even if empty); rationale MUST cite ONLY these computed intersections; expert_models_used MUST equal the runtime-ordered invoked experts for that profile; confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (E) Retroactive fatality & standardized per-profile abstain: before emitting any non-abstain per-profile final output atomically verify that every final field equals the computed intersections/mapping and that final_answer.expert_models_used exactly equals the runtime-ordered call trace. If ANY equality fails (including any pre-call/runtime membership check, any invoked non-candidate expert, expert_models_used mismatch, more than two experts invoked, or any final field not strictly derived from the accepted expert's intersections), TREAT AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile and IMMEDIATELY replace that profile's output with the standardized per-profile abstain JSON EXACTLY (final_label = \"\"; alternative_labels = ordered profile.candidate_labels; predicted_tissue = \"\"; supporting_genes = the pre-call per-profile intersections if available else []; negative_markers = []; expert_models_used = []; confidence = \"low\"; abstain = true). The abstain.rationale MUST be a single concise sentence naming the specific violation and listing offending expert_name(s) when applicable. Do NOT accept, synthesize from, or surface any returned expert outputs for that profile when this retroactive fatal occurs. This rule enforces candidate-only routing, deterministic primary-first fidelity, exactly-one-secondary fallback, strict intersection grounding for all final fields, deterministic confidence mapping, and immediate standardized per-profile abstain on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810751+00:00",
      "updated_at": "2026-06-01T21:54:33.810755+00:00"
    },
    "conflict_and_abstain-00005": {
      "id": "conflict_and_abstain-00005",
      "section": "conflict_and_abstain",
      "content": "conflict_and_abstain-NEW-FATAL-PRIMARY-FIRST-STRICT-ACCEPT: Atomic per-profile primary-first routing_summary & strict-accept contract — For every profile perform one atomic per-profile sequence over ONLY profile.candidate_experts: (A) Pre-call: compute a routing_summary restricted to profile.candidate_experts (per-candidate intersection_count and earliest_index and deterministically chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload together with profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts. Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the failure. (B) Call-first fidelity: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name AND that every invoked expert ∈ profile.candidate_experts. If any runtime membership/first-call check fails (including any invoked non-candidate expert or first-called != chosen_primary), TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned expert outputs for that profile; stop further expert calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with a concise rationale naming the offending expert(s). (C) Post-return strict grounding & acceptance: upon a validated returned expert compute computed_supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and computed_negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; set expert_models_used = the exact runtime-ordered list of invoked experts for that profile. Accept (abstain=false) the primary ONLY when BOTH: (1) returned.final_label unambiguously maps exactly to one provided profile.candidate_label AND (2) computed_supporting_genes is non-empty. If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim), append the secondary to expert_models_used in runtime order, compute intersections for the secondary, and apply the identical post-return checks to the secondary only. Do NOT call more than two experts. (D) Final derivation on accept: when an expert (primary or accepted secondary) is accepted populate every per-profile final field STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapped provided candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = computed_supporting_genes; negative_markers = computed_negative_markers (include even if empty); rationale MUST cite ONLY these computed intersections; expert_models_used MUST equal the runtime-ordered invoked experts for that profile; confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (E) Retroactive fatality & standardized per-profile abstain: before emitting any non-abstain per-profile final output atomically verify that every final field equals the computed intersections/mapping and that final_answer.expert_models_used exactly equals the runtime-ordered call trace. If ANY final field would not equal these computed intersections/mapping OR any pre-call/runtime membership check was violated OR expert_models_used mismatches the runtime trace OR more than two experts were invoked OR evidence was mixed across profiles, TREAT AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile: do NOT accept or synthesize from the unauthorized data and IMMEDIATELY replace that profile's output with the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain.rationale MUST be a single concise sentence naming the specific violation and listing offending expert_name(s) when applicable. Do not mix evidence across profiles or attempt recovery; enforce this rule atomically per profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810761+00:00",
      "updated_at": "2026-06-01T21:54:33.810765+00:00"
    },
    "expert_output_synthesis-00004": {
      "id": "expert_output_synthesis-00004",
      "section": "expert_output_synthesis",
      "content": "Atomic post-accept equality gate — Before emitting any non-abstain per-profile final output, atomically verify that every emitted final field for that profile equals the strict derivation from the accepted expert (primary or accepted deterministic secondary): (1) final.supporting_genes == intersection(accepted_expert.supporting_genes, supplied_ordered_genes); (2) final.negative_markers is present and == intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); (3) final.predicted_tissue == accepted_expert.canonical_tissue (verbatim); (4) final.final_label exactly equals the provided profile.candidate_label mapped from accepted_expert.final_label; and (5) final.expert_models_used exactly equals the runtime-ordered list of invoked experts for that profile. If ANY equality fails (including missing negative_markers or any attempt to add/invent genes or to paraphrase the tissue string), TREAT THIS AS A FATAL SYNTHESIS VIOLATION for that profile and IMMEDIATELY replace that profile's output with the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain.rationale MUST be a single concise sentence naming the specific equality failure (e.g., \"final.supporting_genes not equal to intersection(accepted_expert.supporting_genes, supplied_ordered_genes)\"). Do not attempt recovery, do not mix evidence across profiles, and do not emit any non-abstain final fields prior to this verified equality check.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810772+00:00",
      "updated_at": "2026-06-01T21:54:33.810775+00:00"
    },
    "conflict_and_abstain-00003": {
      "id": "conflict_and_abstain-00003",
      "section": "conflict_and_abstain",
      "content": "fatal-non-candidate-or-first-return-mismatch-atomic-abstain — If at runtime any invoked expert for a profile is NOT a member of profile.candidate_experts OR the first-returned.expert_name != routing_summary.chosen_primary_expert_name, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile and IMMEDIATELY perform these atomic steps: (A) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (B) Stop any further expert calls for that profile; (C) Compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if available else []; (D) Replace the profile's output with the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = the ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (E) Set the abstain.rationale to a single concise sentence naming the specific contract violation and listing offending expert_name(s) (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-returned 'Z' != chosen_primary 'Y'\"). Atomically verify before returning the overall orchestrated response that the affected profile's output equals this exact standardized abstain JSON and replace any differing fields. Do not attempt recovery, do not mix evidence across profiles, and do not surface any data returned from unauthorized experts for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810782+00:00",
      "updated_at": "2026-06-01T21:54:33.810786+00:00"
    },
    "conflict_and_abstain-NEW-FATAL-PRIMARY-FIRST": {
      "id": "conflict_and_abstain-NEW-FATAL-PRIMARY-FIRST",
      "section": "conflict_and_abstain",
      "content": "Atomic fatal-primary-first routing_summary gate + immediate standardized per-profile abstain — For every profile the orchestrator MUST atomically: (1) compute a per-profile routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and deterministically chosen_primary_expert_name) and INCLUDE that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). (2) PRE-CALL verify payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == the computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; if any pre-call check fails DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the failure. (3) Invoke EXACTLY routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime IMMEDIATELY verify the first-returned.expert_name == routing_summary.chosen_primary_expert_name AND that every invoked expert ∈ profile.candidate_experts. IF ANY runtime membership/first-call check fails (including any call to a non-candidate expert or first-called != chosen_primary), THIS IS A FATAL ROUTING VIOLATION for that profile: (A) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (B) Stop further expert calls for that profile; (C) Compute supporting_genes = the pre-call per-profile intersections if available else []; and (D) IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain.rationale MUST be a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Do not attempt recovery, do not mix evidence across profiles, and do not accept or surface any data returned from unauthorized experts for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810792+00:00",
      "updated_at": "2026-06-01T21:54:33.810796+00:00"
    },
    "expert_routing-00064": {
      "id": "expert_routing-00064",
      "section": "expert_routing",
      "content": "Atomic per-profile candidate-only routing → single-profile payload → first-call → strict-grounded-synthesis contract: For every profile perform one atomic sequence over ONLY profile.candidate_experts: (A) Pre-call: compute per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index; deterministically choose routing_summary.chosen_primary_expert_name (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate order). If any candidate has intersection_count>0 restrict chosen_primary to that subset; record applied filters and match_scores in routing_summary. (B) Payload & pre-call gate: include this routing_summary VERBATIM in a single-profile outgoing payload that also carries profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels. Atomically verify BEFORE calling that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true with a single concise rationale naming the violation. (C) Call-order fidelity: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never call non-candidate experts. At runtime immediately verify the first-returned expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert ∈ profile.candidate_experts and record the runtime-ordered call trace. Any deviation is a FATAL ROUTING VIOLATION for that profile: do NOT accept/synthesize from any returned outputs for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (use pre-call intersections if available) with a concise rationale naming the offending expert(s). (D) Post-return strict grounding: compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; set expert_models_used = the exact runtime-ordered list of invoked experts for that profile. Do NOT add, invent, or mix genes/markers/tissues/labels from other experts or profiles. (E) Acceptance & single deterministic secondary: accept (abstain=false) the primary ONLY if BOTH: (1) returned.final_label unambiguously maps exactly to one provided profile.candidate_label AND (2) supporting_genes is non-empty. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim), append secondary to expert_models_used, compute intersections for the secondary, and apply identical checks. Do NOT call more than two experts. (F) Final-field atomic derivation on accept: when an expert is accepted populate final fields STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = computed intersection; negative_markers = computed intersection (include field even if empty); rationale = concise citation limited to those computed intersections only; expert_models_used = exact runtime-ordered invoked experts for that profile; confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (G) Fatal deviations → standardized per-profile abstain: any deviation from this atomic contract (missing/altered routing_summary in payload, payload.candidate_experts != profile.candidate_experts, first-called/returned expert != routing_summary.chosen_primary_expert_name, invocation of any non-candidate expert, final fields not strict intersections of the accepted expert's returned fields with supplied_ordered_genes, evidence mixed across profiles, expert_models_used mismatch with the runtime call trace, calling >2 experts, or invention of markers/tissues) MUST immediately produce the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) and include a concise rationale naming the violated check and offending expert(s). This single atomic rule enforces candidate-only deterministic routing, routing_summary verbatim in payloads, primary-first call fidelity, strict intersection grounding for all emitted fields, single-secondary fallback, deterministic confidence mapping, exact expert_models_used tracing, and immediate per-profile abstain on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810803+00:00",
      "updated_at": "2026-06-01T21:54:33.810807+00:00"
    },
    "conflict_and_abstain-00063": {
      "id": "conflict_and_abstain-00063",
      "section": "conflict_and_abstain",
      "content": "conflict_and_abstain-NEW-FATAL-RUNTIME-MEMBERSHIP-REPLACE: Atomic runtime-membership & expert_models_used enforcement — After any per-profile expert/tool returns (and before emitting or retaining any per-profile final fields) atomically verify: (1) every invoked expert in the runtime call trace ∈ profile.candidate_experts; and (2) final_answer.expert_models_used (if present) exactly equals the runtime-ordered list of invoked experts for that profile. If ANY invoked expert is not a member of profile.candidate_experts OR final_answer.expert_models_used does not exactly match the runtime-invoked ordered experts, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile. Immediately and atomically: (A) discard and DO NOT accept, synthesize from, or reference any returned expert outputs or any previously-emitted per-profile fields for that profile; (B) stop any further expert calls for that profile; (C) compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if those pre-call intersections were computed and available, otherwise supporting_genes = []; (D) replace/emit that profile's output with the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = the ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; and (E) set the abstain.rationale to a single concise sentence naming the violation and listing the offending expert_name(s) (e.g., \"attempted call to non-candidate expert 'tabula_sapiens_spleen_cell_annotation'\"). Do not attempt recovery, do not mix evidence across profiles, and atomically verify the replacement before returning the overall orchestrated response.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810814+00:00",
      "updated_at": "2026-06-01T21:54:33.810818+00:00"
    },
    "conflict_and_abstain-NEW01": {
      "id": "conflict_and_abstain-NEW01",
      "section": "conflict_and_abstain",
      "content": "Atomic per-profile routing_summary gating + strict-intersection + single-secondary fallback + retroactive standardized-abstain: For every profile perform one atomic sequence over ONLY profile.candidate_experts: (A) Pre-call: compute a routing_summary (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the violation. (B) Call-first fidelity: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert ∈ profile.candidate_experts. Any deviation (including any non-candidate call or first-called != chosen_primary) is a FATAL ROUTING VIOLATION: do NOT accept or synthesize from any returned outputs for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (use pre-call intersections if available) with a concise rationale naming offending expert(s). (C) Post-return strict grounding: compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; record expert_models_used as the exact runtime-ordered invoked experts. Do NOT add, invent, or substitute any genes, canonical markers, or tissue strings from domain knowledge or other profiles. (D) Acceptance & single deterministic secondary: accept (abstain=false) the primary ONLY if BOTH: (1) returned.final_label unambiguously maps exactly to one provided profile.candidate_label AND (2) supporting_genes is non-empty. If primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim), append secondary to expert_models_used in runtime order, and apply the identical post-return checks to the secondary only. Do NOT call more than two experts. (E) Retroactive detection of invention or mismatch: before emitting any non-abstain per-profile final output, atomically verify final supporting_genes and negative_markers equal the computed intersections from the accepted expert(s) and that final_answer.expert_models_used exactly equals the runtime call-trace. If any final field is not strictly derived from those intersections (including cases where an accepted expert returned empty supporting_genes but the orchestrator filled supporting_genes from the supplied gene list or other sources), TREAT THIS AS A FATAL ROUTING/SYNTHESIS VIOLATION: do NOT accept or synthesize from the unauthorized data and IMMEDIATELY replace that profile's output with the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain.rationale MUST be a single concise sentence naming the specific violation and listing offending expert_name(s) when applicable (e.g., \"attempted call to non-candidate expert 'X'\" or \"accepted supporting_genes not subset of called expert's returned support\"). Do not attempt recovery, do not mix evidence across profiles, and do not emit non-abstain final fields prior to a validated accepted expert return.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810824+00:00",
      "updated_at": "2026-06-01T21:54:33.810828+00:00"
    },
    "tool_input_restructuring-00062": {
      "id": "tool_input_restructuring-00062",
      "section": "tool_input_restructuring",
      "content": "Atomic per-profile routing_summary + first-call fidelity and intersection-grounded post-return enforcement: For every profile the orchestrator MUST atomically compute a per-profile routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload together with profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels. Pre-call verify payload.candidate_experts == profile.candidate_experts and routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; if this fails DO NOT call any experts and emit the standardized per-profile abstain JSON exactly. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert ∈ profile.candidate_experts; if this fails TREAT AS A FATAL ROUTING VIOLATION and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (do not accept or synthesize from any returned outputs). Upon a validated returned expert compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly and expert_models_used = [returned.expert_name]. Accept (abstain=false) ONLY when supporting_genes is non-empty AND returned.final_label unambiguously maps to one provided candidate_label; otherwise, if a permitted deterministic secondary exists perform EXACTLY ONE deterministic secondary call to the next-ranked candidate using its routing_summary verbatim and apply the same checks; if no accepted expert, IMMEDIATELY emit the standardized per-profile abstain JSON exactly. When accepting, populate final fields strictly from the accepted expert's returned fields intersected with supplied_ordered_genes (final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; supporting_genes and negative_markers = the computed intersections; predicted_tissue = accepted_expert.canonical_tissue exactly; rationale cites only these intersections; expert_models_used = runtime-ordered invoked experts) and set confidence deterministically per the existing rules. Any deviation from these pre-call/payload/first-call/post-return/intersection requirements is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain JSON exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810835+00:00",
      "updated_at": "2026-06-01T21:54:33.810839+00:00"
    },
    "expert_output_synthesis-00061": {
      "id": "expert_output_synthesis-00061",
      "section": "expert_output_synthesis",
      "content": "Atomic per-profile acceptance & mandatory-abstain-on-empty-evidence: For each profile, after the routing_summary.chosen_primary_expert_name is invoked and returns (and at most one deterministic secondary if the primary fails), perform these atomic steps before emitting any non-abstain per-profile fields: (A) Verify returned.expert_name == routing_summary.chosen_primary_expert_name (or equals the single permitted deterministic secondary when used) AND returned.expert_name ∈ profile.candidate_experts; if this fails emit the standardized per-profile abstain JSON exactly. (B) Compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]). (C) Require returned.final_label to unambiguously map exactly to one of profile.candidate_labels AND supporting_genes non-empty to accept the expert (abstain=false). If supporting_genes is empty and profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate and reapply these steps; if no permitted secondary or the secondary also fails, IMMEDIATELY emit the standardized per-profile abstain JSON exactly with supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = runtime-ordered called experts (or [] if fatal), confidence = \"low\", abstain = true and a concise rationale naming the failure and listing canonical markers present in the supplied_ordered_genes but absent from returned.supporting_genes when applicable. (D) On accept: populate final fields STRICTLY from the accepted expert's returned fields intersected with supplied_ordered_genes and exact mapping to provided candidate_labels: final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes and negative_markers = the computed intersections (include negative_markers even if empty); expert_models_used = exact runtime-ordered list of invoked experts for that profile; profile_labels mapping must include profile_id→final_label. (E) Set confidence deterministically: 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (F) Any deviation from these atomic checks (including accepting with empty supporting_genes, emitting final fields not equal to computed intersections, failing to set profile_labels, or expert_models_used mismatch with runtime trace) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain JSON exactly with a concise rationale naming the violated check.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810846+00:00",
      "updated_at": "2026-06-01T21:54:33.810850+00:00"
    },
    "expert_routing-NEW-PRIMARY_MUST": {
      "id": "expert_routing-NEW-PRIMARY_MUST",
      "section": "expert_routing",
      "content": "Atomic primary-first routing_and_no-internal-synthesis contract — For every profile: (A) compute a routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and the deterministically chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). (B) Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If any pre-call check fails, DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON exactly: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true, with a concise rationale naming the failure. (C) Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; do NOT call non-candidate experts. (D) At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that it ∈ profile.candidate_experts. If this verification fails, TREAT AS A FATAL ROUTING VIOLATION and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (use pre-call intersections if available) with a concise rationale naming the offending expert. (E) For a validated returned expert compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; set expert_models_used = the exact runtime-ordered list of invoked experts for that profile. (F) Accept (abstain=false) ONLY when returned.final_label unambiguously maps exactly to one of the provided profile.candidate_labels AND supporting_genes is non-empty. When accepting, populate every final field STRICTLY from the accepted expert's returned fields intersected with supplied_ordered_genes (final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes and negative_markers = the computed intersections; rationale cites only these intersections; expert_models_used = runtime-ordered invoked experts; confidence per deterministic mapping). (G) If any step or equality check in (A)-(F) fails (including missing/altered routing_summary, payload.candidate_experts mismatch, first-called != chosen_primary, any invoked expert ∉ profile.candidate_experts, final fields not strict intersections of the accepted expert's returned fields with supplied_ordered_genes, predicted_tissue mismatch, or expert_models_used mismatch with the runtime trace), DO NOT synthesize; IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with fields: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true, and a single concise rationale naming the violated check and offending expert(s). This rule forbids internal heuristic labeling prior to a validated expert return and enforces strict intersection-grounded synthesis or standardized abstain on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810856+00:00",
      "updated_at": "2026-06-01T21:54:33.810860+00:00"
    },
    "expert_output_synthesis-00060": {
      "id": "expert_output_synthesis-00060",
      "section": "expert_output_synthesis",
      "content": "Atomic single-expert-first synthesis enforcement — For every profile perform one atomic per-profile sequence before emitting any non-abstain final fields: (A) Pre-call: compute a routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index plus chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also include profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). (B) Call: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. (C) Runtime-check: if the runtime call trace shows ZERO invoked experts for a profile with non-empty profile.candidate_experts OR the first-called expert_name != routing_summary.chosen_primary_expert_name OR any invoked expert ∉ profile.candidate_experts, TREAT AS A FATAL ROUTING VIOLATION for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY: final_label = \"\"; alternative_labels = ordered profile.candidate_labels; predicted_tissue = \"\"; supporting_genes = the pre-call computed per-profile intersections if available else []; negative_markers = []; expert_models_used = []; confidence = \"low\"; abstain = true. Rationale must concisely name the violation (e.g., \"no expert called; internal labeling attempted\" or \"attempted call to non-candidate expert 'X'\"). (D) Post-return grounding on a validated returned expert (returned.expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts) and at most one deterministic secondary if primary fails acceptance: compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; map returned.final_label to one of the provided profile.candidate_labels for final_label. (E) Acceptance: accept (abstain=false) ONLY when supporting_genes non-empty AND returned.final_label unambiguously maps to a provided candidate_label (apply canonical-marker gate as applicable). (F) Synthesis constraints: when accepting, derive every final field STRICTLY from the accepted expert's returned fields intersected with supplied_ordered_genes (supporting_genes/negative_markers must equal the computed intersections), set expert_models_used to the exact runtime-ordered list of invoked experts for that profile, build the rationale citing only those intersections, and set confidence deterministically ('high' iff reported_confidence == 'high' AND supporting_genes contains ≥1 canonical routing marker; else 'moderate' iff supporting_genes non-empty; else 'low'). (G) Any deviation from this atomic contract (missing/altered routing_summary in payload, no expert called, first-called != chosen_primary, invocation of non-candidate expert, final fields not strict intersections of the accepted expert's returned fields with supplied_ordered_genes, expert_models_used mismatch with runtime trace, mixing evidence across profiles, or calling >2 experts) MUST immediately produce the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check. This rule enforces mandatory primary-first expert invocation and single-expert grounding and resolves bypass/mixing failure modes.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810866+00:00",
      "updated_at": "2026-06-01T21:54:33.810870+00:00"
    },
    "conflict_and_abstain-00099": {
      "id": "conflict_and_abstain-00099",
      "section": "conflict_and_abstain",
      "content": "conflict_and_abstain-00099: Atomic routing_summary gating + fatal non-candidate-call abstain — For every profile the orchestrator MUST atomically compute a per-profile routing_summary over ONLY profile.candidate_experts and include that routing_summary VERBATIM in the single-profile outgoing payload before any expert/tool invocation. Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name (which must be a member of profile.candidate_experts). Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify that the first-returned expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert ∈ profile.candidate_experts. If ANY of these checks fails (missing/altered routing_summary in payload; payload.candidate_experts != profile.candidate_experts; first-called != chosen_primary; or any invoked expert not in profile.candidate_experts) TREAT THIS AS A FATAL ROUTING VIOLATION for that profile and DO THE FOLLOWING ATOMIC STEPS: (A) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (B) Stop any further expert calls for that profile; (C) Compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if those pre-call intersections were computed and available, else supporting_genes = []; (D) Immediately emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (E) Rationale MUST be a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (e.g., \"payload.routing_summary mismatch; attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Do not attempt recovery, do not call non-candidate experts for that profile, and do not mix or reuse evidence from other profiles when producing the abstain output.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810877+00:00",
      "updated_at": "2026-06-01T21:54:33.810881+00:00"
    },
    "tool_input_restructuring-00059": {
      "id": "tool_input_restructuring-00059",
      "section": "tool_input_restructuring",
      "content": "Atomic pre-call routing_summary + first-call-fidelity enforcement: For every single-profile tool invocation compute a routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify PRE-CALL that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == the orchestrator-computed routing_summary and intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert is a member of profile.candidate_experts. If ANY pre-call or runtime check fails (including missing/altered routing_summary, payload.candidate_experts mismatch, first-called != chosen_primary_expert_name, or any invoked expert not in profile.candidate_experts) TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned outputs for that profile; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain.rationale MUST be a single concise sentence naming the specific violation and listing offending expert_name(s) when applicable (e.g., \"first-called 'X' != chosen_primary 'Y'\" or \"payload.routing_summary mismatch; attempted call to non-candidate expert 'Z'\"). Do not attempt recovery, do not mix evidence across profiles, and do not accept any returned outputs for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810888+00:00",
      "updated_at": "2026-06-01T21:54:33.810891+00:00"
    },
    "expert_routing-NEW-MANDATORY_PER_PROFILE_EXPERT_CALL": {
      "id": "expert_routing-NEW-MANDATORY_PER_PROFILE_EXPERT_CALL",
      "section": "expert_routing",
      "content": "Mandatory per-profile expert invocation & strict post-call derivation: For every profile with non-empty profile.candidate_experts the orchestrator MUST (1) compute the deterministic chosen_primary_expert over ONLY profile.candidate_experts (use existing tie-breaks), include that routing_summary verbatim in a single-profile payload, and invoke exactly that chosen_primary_expert as a single-profile call (do not call experts with no profile_id or make multi-profile calls); (2) wait for and atomically verify the expert return before emitting any non-abstain per-profile fields; (3) derive every accepted final field strictly from the accepted expert's returned fields intersected with the supplied_ordered_genes (supporting_genes = intersection(expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); predicted_tissue = expert.canonical_tissue verbatim; final_label = exact mapping of expert.final_label to one provided candidate_label); (4) include final_answer.expert_models_used equal exactly to the runtime-ordered list of invoked experts for that profile; and (5) if at runtime ZERO experts were invoked for a profile with non-empty candidate_experts, or any invoked expert is not in profile.candidate_experts, or any final field is not strictly derived from the accepted expert's intersections/mapping, treat this as a fatal routing violation for that profile and immediately emit the standardized per-profile abstain JSON exactly. Do not substitute internal heuristics or cross-profile evidence to produce labels.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810898+00:00",
      "updated_at": "2026-06-01T21:54:33.810902+00:00"
    },
    "expert_output_synthesis-NEW05": {
      "id": "expert_output_synthesis-NEW05",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-NEW05: Atomic primary-call-first + single-expert-grounding rule — For any profile with non-empty profile.candidate_experts the orchestrator MUST: (A) compute and include the per-profile routing_summary (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) verbatim in the single-profile outgoing payload and invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call; (B) NOT emit any non-abstain per-profile final fields (final_label, predicted_tissue, supporting_genes, negative_markers, rationale, expert_models_used, confidence) prior to receiving and grounding that called expert's return; (C) upon a valid returned expert (returned.expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts) compute: supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly; map returned.final_label to one of the provided profile.candidate_labels and set final_label = that provided candidate_label; set expert_models_used = the exact runtime-ordered list of invoked experts for that profile; build rationale citing only the computed supporting_genes and negative_markers intersections; set confidence deterministically: 'high' iff returned.reported_confidence == 'high' AND supporting_genes contains ≥1 returned.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (D) Acceptance & fallback: accept (abstain=false) ONLY when returned.final_label unambiguously maps to a provided candidate_label AND supporting_genes non-empty. If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim), append the secondary to expert_models_used in runtime order, compute its intersections and apply the identical checks once. If no permitted secondary exists or the secondary also fails acceptance, IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = the runtime-ordered called experts (or [] if fatal), confidence = \"low\", abstain = true) and include a concise rationale naming the failure (e.g., \"supporting_genes empty\" or \"returned.final_label not in candidate_labels\"). (E) Prohibitions & fatal handling: under no circumstance derive final fields from internal heuristics or from experts not in profile.candidate_experts, invent/add markers or tissues, or mix evidence across profiles. Any deviation from this atomic contract (missing/altered routing_summary in payload, first-called != chosen_primary_expert_name, any invoked expert ∉ profile.candidate_experts, final fields not strict intersections/mappings, or expert_models_used mismatching runtime call trace) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain JSON exactly with a concise rationale naming the violation.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810912+00:00",
      "updated_at": "2026-06-01T21:54:33.810915+00:00"
    },
    "expert_output_synthesis-00058": {
      "id": "expert_output_synthesis-00058",
      "section": "expert_output_synthesis",
      "content": "Atomic primary-evidence-first routing→call→synthesis contract (single-profile): For every profile, compute a routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Pre-call verify routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; if not, DO NOT call and immediately emit the standardized per-profile abstain JSON. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name (or equals the single permitted deterministic secondary when a deterministic secondary was invoked) AND that every invoked expert is a member of profile.candidate_experts; any violation (including any non-candidate expert invoked or first-called != chosen_primary) is a fatal routing violation for that profile and MUST produce the standardized per-profile abstain JSON exactly with a concise rationale naming the violation and offending expert(s). Accept (abstain=false) only from one accepted expert (primary if accepted, otherwise the single deterministic secondary). When accepting, derive every final field strictly from that accepted expert's returned fields intersected with the supplied_ordered_genes and by mapping accepted_expert.final_label to one of the provided candidate_labels: final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); predicted_tissue = accepted_expert.canonical_tissue verbatim; expert_models_used = exact runtime-ordered list of invoked experts for that profile. Do NOT add, invent, or mix genes/markers/tissues/labels across experts or profiles. If primary fails acceptance and profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim) and apply the identical checks; do not call more than two experts. Deterministic confidence mapping: 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. Any deviation from these atomic obligations (missing/altered routing_summary, payload.candidate_experts mismatch, first-called != chosen_primary, invocation of non-candidate expert, evidence mixed across experts/profiles, final fields not strict intersections/mappings, or expert_models_used not equal to runtime call trace) MUST produce the standardized per-profile abstain JSON exactly and stop further calls for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810920+00:00",
      "updated_at": "2026-06-01T21:54:33.810923+00:00"
    },
    "tool_input_restructuring-00057": {
      "id": "tool_input_restructuring-00057",
      "section": "tool_input_restructuring",
      "content": "Atomic routing_summary pre-call + first-call fidelity enforcement: For every single-profile request compute a routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name). Include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels) and atomically verify BEFORE calling that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == the computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify first-called returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert is a member of profile.candidate_experts. If any pre-call or runtime check fails (missing/altered routing_summary, payload.candidate_experts mismatch, first-called != chosen_primary, or any invoked expert not in profile.candidate_experts) TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from any returned outputs for that profile; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with fields: final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain.rationale MUST be a concise sentence naming the specific violation and the offending expert_name(s) when applicable (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Do not attempt recovery or mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810928+00:00",
      "updated_at": "2026-06-01T21:54:33.810931+00:00"
    },
    "expert_routing-00056": {
      "id": "expert_routing-00056",
      "section": "expert_routing",
      "content": "Atomic candidate-only routing_summary gate + fatal standardized per-profile abstain: For every profile the orchestrator MUST atomically compute a per-profile routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and deterministically chosen_primary_expert_name) and INCLUDE that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). Pre-call verify payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; if any pre-call check fails DO NOT call any experts and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a single concise rationale naming the failure. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime IMMEDIATELY verify the first-returned.expert_name == routing_summary.chosen_primary_expert_name AND that every invoked expert ∈ profile.candidate_experts. If ANY runtime membership/first-call check fails (including any invoked non-candidate expert or first-called != chosen_primary), TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned expert outputs for that profile; stop further expert calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with a concise rationale naming the offending expert_name(s) (e.g., \"attempted call to non-candidate expert 'X'\"). Do NOT mix evidence across profiles or attempt recovery for the affected profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810936+00:00",
      "updated_at": "2026-06-01T21:57:42.105414+00:00"
    },
    "tool_input_restructuring-00055": {
      "id": "tool_input_restructuring-00055",
      "section": "tool_input_restructuring",
      "content": "Atomic per-profile payload & first-call fidelity: For every profile compute a routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name). Build a single-profile tool payload that includes verbatim: profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, payload.candidate_experts, and the computed routing_summary. Atomically verify BEFORE calling that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. At runtime immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name and that every invoked expert is a member of profile.candidate_experts. If any pre-call or runtime check fails (including any invoked expert ∉ profile.candidate_experts or first-called != chosen_primary_expert_name) TREAT THIS AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned expert outputs for that profile; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON exactly with fields: final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. Rationale in the abstain must concisely name the violation and list offending expert_name(s) (e.g., \"attempted call to non-candidate expert 'X'\" or \"first-called 'Z' != chosen_primary 'Y'\"). Do not attempt recovery, do not mix evidence across profiles, and do not accept or surface any data returned from unauthorized experts for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810943+00:00",
      "updated_at": "2026-06-01T21:54:33.810946+00:00"
    },
    "conflict_and_abstain-00054": {
      "id": "conflict_and_abstain-00054",
      "section": "conflict_and_abstain",
      "content": "atomic-fatal-routing-abstain+single-sentence-rationale: Before invoking any expert for a profile the orchestrator MUST compute and embed the per-profile routing_summary verbatim in the single-profile payload and atomically verify that routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. If at runtime any of the following is observed: (1) payload.routing_summary missing or != computed routing_summary, (2) routing_summary.chosen_primary_expert_name was not the first-called expert, OR (3) any invoked expert is not a member of profile.candidate_experts, TREAT THIS AS A FATAL ROUTING VIOLATION for that profile. Immediately and atomically: (A) do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (B) stop any further expert calls for that profile; (C) compute supporting_genes = the pre-call per-profile intersections if available else []; and (D) EMIT the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = the ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true. The abstain.rationale MUST be a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (example format: \"routing_summary missing; attempted call to non-candidate expert 'X'\"). Replace any previously-emitted per-profile fields with this exact standardized abstain output. Do not attempt recovery, do not call non-candidate experts, and do not mix evidence across profiles.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810951+00:00",
      "updated_at": "2026-06-01T21:54:33.810954+00:00"
    },
    "expert_routing-00053": {
      "id": "expert_routing-00053",
      "section": "expert_routing",
      "content": "Atomic per-profile deterministic routing_summary and mandatory post-call grounding: For every profile (only over profile.candidate_experts) compute per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index = smallest matched index (or null). Deterministically choose chosen_primary_expert by: highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate_experts list order. Build a routing_summary listing each candidate's intersection_count and earliest_index and include chosen_primary_expert_name and its metrics; include this routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels). Atomically verify BEFORE any call that payload.candidate_experts == profile.candidate_experts AND payload.routing_summary == computed routing_summary AND intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. Upon any expert return immediately verify returned.expert_name == routing_summary.chosen_primary_expert_name AND returned.expert_name ∈ profile.candidate_experts; if this fails treat as a fatal routing violation for that profile and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation. For a valid returned expert compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) and set predicted_tissue = returned.canonical_tissue verbatim. Accept (abstain=false) the primary ONLY if BOTH: (A) returned.final_label unambiguously maps exactly to one of profile.candidate_labels AND (B) supporting_genes is non-empty AND (supporting_genes contains ≥1 returned.canonical_routing_marker OR the returned.final_label mapping is unambiguous). If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (build/send its routing_summary verbatim), append the secondary to expert_models_used in runtime order, compute its intersections and apply the identical checks once. If neither called expert is accepted or any pre-call/post-return/payload/runtime contract fails, IMMEDIATELY emit the standardized per-profile abstain JSON exactly. When accepting, populate final fields STRICTLY from the accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapped candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = computed intersection; negative_markers = computed intersection (include field even if empty); expert_models_used = exact runtime-ordered invoked experts for that profile; rationale = concise citation limited to supporting_genes and negative_markers intersections only; confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. Any deviation from these atomic obligations (including calling non-candidate experts, missing/altered routing_summary in payload, first-called != chosen_primary_expert_name, evidence not strict intersections, evidence mixing across profiles, inventing markers/tissues/labels, or expert_models_used mismatch with runtime trace) is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain JSON exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810959+00:00",
      "updated_at": "2026-06-01T21:54:33.810961+00:00"
    },
    "expert_output_synthesis-00052": {
      "id": "expert_output_synthesis-00052",
      "section": "expert_output_synthesis",
      "content": "Atomic per-profile routing→call→synthesis contract (consolidated): For every profile perform one atomic sequence over ONLY profile.candidate_experts: (1) Pre-call: compute per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index; deterministically choose routing_summary.chosen_primary_expert_name (highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate list order). If any candidate has intersection_count>0 restrict the chosen_primary to that subset. Include this routing_summary VERBATIM in a single-profile payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels) and verify pre-call that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary. If any pre-call check fails, do NOT call any experts and emit the standardized per-profile abstain JSON exactly. (2) Call-order: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. Record runtime-ordered invoked experts. If at runtime the first-called expert != chosen_primary or any invoked expert ∉ profile.candidate_experts, TREAT AS A FATAL ROUTING VIOLATION and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (use pre-call intersections if available) with a concise rationale naming the violation. (3) Post-return grounding: upon an allowed expert return verify returned.expert_name == routing_summary.chosen_primary_expert_name (or equals the single permitted deterministic secondary when a secondary was invoked) and that it is a member of profile.candidate_experts. Compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly. Do NOT add, invent, or mix genes/markers/tissues/labels across experts or profiles. (4) Acceptance gating & single-secondary fallback: accept (abstain=false) the primary ONLY if BOTH (A) returned.final_label unambiguously maps exactly to one of the provided profile.candidate_labels AND (B) supporting_genes is non-empty. If the primary fails acceptance AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim), append it to expert_models_used in runtime order, compute the same intersections for the secondary, and apply identical mapping+supporting_genes checks. Do NOT call more than two experts. (5) On accept: populate final fields STRICTLY from the accepted expert's returned fields intersected with supplied_ordered_genes and from the exact mapping to provided candidate_labels: final_label = mapped provided candidate_label; alternative_labels = ordered profile.candidate_labels; predicted_tissue = accepted_expert.canonical_tissue exactly; supporting_genes = intersection(accepted_expert.supporting_genes, supplied_ordered_genes); negative_markers = intersection(accepted_expert.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (include field even if empty); expert_models_used = exact runtime-ordered invoked experts for that profile; rationale = concise citation limited to those computed supporting_genes and negative_markers intersections only; confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (6) Fatal deviations → standardized per-profile abstain: if neither primary nor permitted secondary is accepted OR any pre-call/payload/runtime/post-return contract check fails (including missing/altered routing_summary, payload.candidate_experts mismatch, first-called != chosen_primary, invocation of non-candidate expert, returned.final_label not mapping to provided candidate_labels, supporting_genes/negative_markers/predicted_tissue not strict intersections, evidence mixed across profiles, expert_models_used mismatch with runtime trace, or calling >2 experts), IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY: final_label=\"\"; alternative_labels = ordered profile.candidate_labels; predicted_tissue = \"\"; supporting_genes = the pre-call computed intersections if available else []; negative_markers = [] or computed intersections if present; expert_models_used = [] (if fatal pre-/runtime contract failure) or the runtime-ordered called experts when valid calls occurred; confidence = \"low\"; abstain = true. Include a concise rationale naming the violated check or offending expert(s). This rule is atomic and forbids invention or cross-profile evidence mixing.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810966+00:00",
      "updated_at": "2026-06-01T21:54:33.810969+00:00"
    },
    "expert_output_synthesis-00051": {
      "id": "expert_output_synthesis-00051",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-NEW_ATOMIC_POSTRETURN_GATE: For every profile, immediately after any expert/tool returns perform this single atomic per-profile sequence before emitting any final fields: (1) verify returned.expert_name ∈ profile.candidate_experts AND returned.expert_name == routing_summary.chosen_primary_expert_name (or equals the single permitted deterministic secondary when a secondary was invoked). If this membership/first-call check fails, treat as a fatal routing violation and emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation. (2) Compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly. (3) Require returned.final_label to unambiguously map exactly to one of profile.candidate_labels AND supporting_genes must be non-empty to accept this expert (abstain=false). When accepting, set final_label = mapped provided candidate_label; alternative_labels = ordered profile.candidate_labels; supporting_genes and negative_markers = the computed intersections (include negative_markers even if empty); expert_models_used = the exact runtime-ordered list of experts actually invoked for that profile; rationale = concise citation restricted to the computed supporting_genes and negative_markers only; confidence = 'high' iff returned.reported_confidence == 'high' AND supporting_genes contains ≥1 returned.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (4) If the primary fails the mapping or supporting_genes gate AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate (send its routing_summary verbatim), append the secondary to expert_models_used in runtime order, and repeat steps (1)-(3) for the secondary. Do NOT call more than two experts. (5) If neither the primary nor the permitted secondary is accepted, OR any of the post-return atomic checks (membership, mapping, intersection derivation, expert_models_used equality to runtime trace, or strict use of intersections) fails, IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections if available else [], negative_markers = [] or the computed intersection if present, expert_models_used = [] (if fatal pre-/runtime contract failure) or the runtime-ordered called experts when valid calls occurred, confidence = \"low\", abstain = true) with a concise rationale naming the violated check. (6) Under no circumstance add, invent, or mix genes/markers/tissues/labels across experts or profiles; all final supporting_genes, negative_markers, predicted_tissue, final_label, rationale, and expert_models_used must be derived exactly as above. This rule is atomic and mandatory per profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810973+00:00",
      "updated_at": "2026-06-01T21:54:33.810976+00:00"
    },
    "evidence_grounding-00050": {
      "id": "evidence_grounding-00050",
      "section": "evidence_grounding",
      "content": "Mandatory-canonical-marker-acceptance-gate: After a permitted primary (and at most one deterministic secondary) expert return, compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and compute canonical_overlap = intersection(supporting_genes, accepted_expert.canonical_routing_markers). Accept (abstain=false) ONLY when BOTH (1) returned.final_label unambiguously maps exactly to one of the provided profile.candidate_labels AND (2) supporting_genes is non-empty AND canonical_overlap is non-empty (i.e., supporting_genes includes >=1 accepted_expert.canonical_routing_marker). If the primary fails this gate and profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim) and apply the identical canonical-marker gate to the secondary. If no permitted secondary exists or the secondary also fails the gate, IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call computed intersections if available else [], negative_markers = [] or computed intersections if present, expert_models_used = runtime-ordered called experts (or [] if fatal), confidence = \"low\", abstain = true) and include a concise rationale naming the gate failure (e.g., \"no canonical routing marker overlap\"). Always include negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]) (may be empty) in every non-abstain output and require final_answer.expert_models_used to exactly match the runtime-ordered invoked experts when accepting. Any deviation from this canonical-marker acceptance gate is a fatal routing/synthesis violation for that profile and MUST produce the standardized per-profile abstain JSON exactly.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810981+00:00",
      "updated_at": "2026-06-01T21:54:33.810984+00:00"
    },
    "expert_output_synthesis-00049": {
      "id": "expert_output_synthesis-00049",
      "section": "expert_output_synthesis",
      "content": "atomic_candidate-only_routing_and_strict_postcall_verification: For every profile perform one atomic sequence restricted to ONLY profile.candidate_experts: (A) Pre-call: compute and include a routing_summary over profile.candidate_experts (per-candidate intersection_count and earliest_index and chosen_primary_expert_name) in the single-profile payload and verify payload.candidate_experts == profile.candidate_experts before calling. (B) Call-order: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; do not call non-candidate experts. (C) Immediate post-return verification: upon any expert return verify returned.expert_name == routing_summary.chosen_primary_expert_name AND returned.expert_name ∈ profile.candidate_experts. If this verification fails, TREAT AS A FATAL ROUTING VIOLATION for that profile: do NOT accept or synthesize from that expert's outputs; stop further calls for that profile; compute supporting_genes = the pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections or [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violation and offending expert. (D) Evidence grounding & mapping: compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); require that returned.final_label unambiguously maps exactly to one provided profile.candidate_label. (E) Acceptance gating & deterministic secondary: accept (abstain=false) the primary ONLY if BOTH (1) supporting_genes non-empty AND (2) returned.final_label maps to a provided candidate_label. If primary fails acceptance AND profile.candidate_experts.size > 1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (send its routing_summary verbatim), append it to expert_models_used, repeat steps (C)-(E) for the secondary, and do NOT call more than two experts. (F) Final-field derivation on accept: when an expert (primary or accepted secondary) is accepted populate final fields STRICTLY from that accepted expert's returned fields intersected with supplied_ordered_genes only: final_label = mapped provided candidate_label (no freeform labels), alternative_labels = ordered profile.candidate_labels, predicted_tissue = accepted_expert.canonical_tissue exactly, supporting_genes = computed intersection, negative_markers = computed intersection (include field even if empty), expert_models_used = exact runtime-ordered list of invoked experts for that profile, rationale = concise citation limited to the computed supporting_genes and negative_markers intersections, abstain=false. (G) Deterministic confidence mapping: set confidence = 'high' iff accepted_expert.reported_confidence == 'high' AND supporting_genes contains ≥1 accepted_expert.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (H) Fatal deviations → standardized per-profile abstain: any deviation from these atomic obligations (pre-call payload mismatch, first-called/returned expert != chosen_primary_expert_name, invoking non-candidate experts, returned.final_label not mapping to a provided candidate_label, final supporting_genes/negative_markers not equal to the computed intersections, omission of negative_markers, expert_models_used not equal to runtime call-trace, mixing evidence across profiles, calling >2 experts, or accepting freeform labels/tissues) MUST produce the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the violated check or offending expert(s).",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810989+00:00",
      "updated_at": "2026-06-01T21:54:33.810991+00:00"
    },
    "expert_output_synthesis-00048": {
      "id": "expert_output_synthesis-00048",
      "section": "expert_output_synthesis",
      "content": "expert_output_synthesis-NEW-PRIMARY_INTERSECTION_GATE: Atomic per-profile primary-first intersection+mapping gate (candidate-only, single-secondary fallback, exact runtime trace, standardized per-profile abstain). For every profile perform one atomic routing→payload→call→post-return→synthesis sequence over ONLY profile.candidate_experts: (1) Pre-call: compute per-candidate intersection_count = |intersection(expert.canonical_routing_markers, supplied_ordered_genes)| and earliest_index; deterministically choose chosen_primary_expert from profile.candidate_experts by: highest intersection_count → tie-break smallest earliest_index → tie-break exact profile.tissue match → tie-break candidate_experts list order. Build a routing_summary listing each candidate's intersection_count and earliest_index and include chosen_primary_expert_name; include this routing_summary VERBATIM in the single-profile tool payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, and ordered candidate_labels). Atomically verify BEFORE the call that payload.candidate_experts == profile.candidate_experts and payload.routing_summary == computed routing_summary; if any pre-call check fails, do NOT call any expert and IMMEDIATELY emit the standardized per-profile abstain JSON exactly (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true) with a concise rationale naming the failure. (2) Call-order and first-call fidelity: invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile; never invoke non-candidate experts. If at runtime any invoked expert is not a member of profile.candidate_experts OR the runtime first-called expert_name != routing_summary.chosen_primary_expert_name, TREAT AS A FATAL ROUTING VIOLATION for that profile: do NOT accept, synthesize from, or reference any returned outputs for that profile; stop further calls for that profile; compute supporting_genes = pre-call per-profile intersections if available else []; and IMMEDIATELY emit the standardized per-profile abstain JSON exactly with a concise rationale naming the offending expert(s). (3) Immediate post-return atomic grounding: upon a valid expert return verify returned.expert_name == routing_summary.chosen_primary_expert_name and ∈ profile.candidate_experts; compute supporting_genes = intersection(returned.supporting_genes, supplied_ordered_genes) and negative_markers = intersection(returned.negative_markers, supplied_ordered_genes[, user_provided_negative_markers if present]); set predicted_tissue = returned.canonical_tissue exactly and set expert_models_used = [first_expert_name] at this point. Do NOT add, invent, or mix genes/markers/tissues/labels from other experts or profiles. (4) Atomic acceptance gating (primary): accept (abstain=false) the primary ONLY when BOTH: (A) returned.final_label unambiguously maps exactly to one provided profile.candidate_label; AND (B) supporting_genes is non-empty (supporting_genes == intersection(returned.supporting_genes, supplied_ordered_genes)). When accepting, set final_label = mapped provided candidate_label, alternative_labels = ordered profile.candidate_labels, supporting_genes and negative_markers = the computed intersections (include negative_markers even if empty), predicted_tissue = returned.canonical_tissue exactly, expert_models_used = exact runtime-ordered list of invoked experts for that profile, rationale = concise citation limited to these computed supporting_genes and negative_markers intersections, and confidence = 'high' iff returned.reported_confidence == 'high' AND supporting_genes contains ≥1 returned.canonical_routing_marker; else 'moderate' iff supporting_genes non-empty; else 'low'. (5) Deterministic single-secondary fallback: If the primary fails either acceptance check AND profile.candidate_experts.size>1 perform EXACTLY ONE deterministic secondary call to the next-ranked candidate_expert (by next-largest intersection_count using same tie-breaks). Build and send a single-profile payload for the secondary that includes that secondary-call routing_summary VERBATIM; append the secondary to expert_models_used in runtime order; upon secondary return repeat the post-return verification and the identical acceptance gating and intersection derivations for the secondary. Do NOT call more than two experts for a profile. (6) Mandatory exact expert_models_used tracing: If accepting (abstain=false), final_answer.expert_models_used MUST exactly equal the runtime-ordered list of invoked experts for that profile; any mismatch is a fatal violation and MUST produce the standardized per-profile abstain JSON exactly. (7) Fatal deviations & standardized per-profile abstain: If neither primary nor permitted secondary is accepted OR any pre-call/payload/runtime/post-return contract check fails (including calling non-candidate experts, payload.routing_summary mismatch, first-called != chosen_primary_expert_name, returned.final_label not in candidate_labels, supporting_genes empty, evidence-mixing across profiles, invented markers/tissues, omitted negative_markers, or expert_models_used mismatch), IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY (final_label=\"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = pre-call computed intersections if available else [], negative_markers = [] or the computed intersection if present, expert_models_used = [] (if fatal pre-/runtime contract failure) or the runtime-ordered invoked experts when valid calls occurred, confidence = \"low\", abstain = true) and include a concise rationale naming the violated check and offending expert(s). This single atomic rule enforces deterministic candidate-only routing, routing_summary verbatim in payloads, single-primary first-call fidelity, single deterministic secondary fallback, strict intersection+mapping grounding for all emitted fields, exact expert_models_used tracing, deterministic confidence, mandatory negative_markers emission, and standardized per-profile abstain on any contract breach.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.810996+00:00",
      "updated_at": "2026-06-01T21:54:33.811425+00:00"
    },
    "conflict_and_abstain-00004": {
      "id": "conflict_and_abstain-00004",
      "section": "conflict_and_abstain",
      "content": "conflict_and_abstain-NEW-FATAL-FIRST-RETURN-FIDELITY: Atomic per-profile primary-first runtime-first-return enforcement + fatal standardized per-profile abstain — For every profile the orchestrator MUST atomically compute a per-profile routing_summary over ONLY profile.candidate_experts (per-candidate intersection_count and earliest_index and deterministically chosen_primary_expert_name) and include that routing_summary VERBATIM in the single-profile outgoing payload (payload must also carry profile_id, question_text, supplied_ordered_genes, task_family/type, grounded_context, ordered candidate_labels, and payload.candidate_experts == profile.candidate_experts). Pre-call gating: BEFORE invoking any expert atomically verify ALL: (A) payload.candidate_experts == profile.candidate_experts; (B) payload.routing_summary exactly equals the orchestrator-computed routing_summary; AND (C) intended expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts. Invoke exactly routing_summary.chosen_primary_expert_name as the first runtime call for that profile. Runtime-first-return fidelity: IMMEDIATELY upon the first expert return verify that returned.expert_name == routing_summary.chosen_primary_expert_name AND that every invoked expert in the runtime call trace ∈ profile.candidate_experts. Fatal handling: IF ANY pre-call or runtime check fails (including missing/altered routing_summary in payload, payload.candidate_experts mismatch, first-called != chosen_primary, or any invoked expert not in profile.candidate_experts) TREAT THIS AS A FATAL ROUTING VIOLATION for that profile and PERFORM THESE ATOMIC STEPS: (1) Do NOT accept, synthesize from, or reference any returned expert outputs for that profile; (2) Stop any further expert calls for that profile; (3) Compute supporting_genes = the pre-call per-profile intersections (intersection(expert.canonical_routing_markers, supplied_ordered_genes)) if those pre-call intersections were computed and available, otherwise supporting_genes = []; (4) IMMEDIATELY emit the standardized per-profile abstain JSON EXACTLY with these fields: final_label = \"\", alternative_labels = the ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = computed pre-call intersections (or []), negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true; (5) Set the abstain.rationale to a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (e.g., \"first-called 'tabula_sapiens_eye_cell_annotation' != chosen_primary 'tabula_sapiens_small_intestine_cell_annotation'\"). Atomically verify before returning the overall orchestrated response that the affected profile's output equals this exact standardized abstain JSON; if any field differs, replace it with the exact standardized abstain JSON. Do not attempt recovery, do not mix evidence across profiles, and do not accept or surface any data returned from unauthorized experts for that profile.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:54:33.811430+00:00",
      "updated_at": "2026-06-01T21:54:33.811433+00:00"
    },
    "conflict_and_abstain-NEW02": {
      "id": "conflict_and_abstain-NEW02",
      "section": "conflict_and_abstain",
      "content": "conflict_and_abstain-NEW02: Mandatory atomic post-orchestration enforcement — Before accepting any non-abstain per-profile final output the orchestrator MUST atomically verify all of the following for that profile: (1) the outgoing single-profile payload included the routing_summary verbatim and the runtime-first-called expert_name == routing_summary.chosen_primary_expert_name ∈ profile.candidate_experts; (2) final_answer.expert_models_used is present and exactly equals the runtime-ordered list of experts actually invoked for that profile; and (3) the final_answer.negative_markers field is present (may be empty) and equals the computed intersection per evidence_grounding rules. If ANY of these checks fails (including missing routing_summary, first-called != chosen_primary, any invoked expert not in profile.candidate_experts, expert_models_used mismatch, or missing/omitted negative_markers) TREAT THIS AS A FATAL ROUTING/SYNTHESIS VIOLATION for that profile and IMMEDIATELY perform an atomic replacement of that profile's output with the standardized per-profile abstain JSON EXACTLY (final_label = \"\", alternative_labels = ordered profile.candidate_labels, predicted_tissue = \"\", supporting_genes = the pre-call per-profile intersections if available else [], negative_markers = [], expert_models_used = [], confidence = \"low\", abstain = true). The abstain.rationale MUST be a single concise sentence naming the specific contract violation and listing offending expert_name(s) when applicable (e.g., \"attempted call to non-candidate expert 'X'\" or \"missing negative_markers field\"). Do NOT accept, synthesize from, or surface any returned expert outputs for that profile when this fatal violation occurs; stop further expert calls for that profile; and atomically verify the standardized abstain JSON before returning the overall response.",
      "helpful": 1,
      "harmful": 0,
      "neutral": 0,
      "created_at": "2026-06-01T21:59:17.353263+00:00",
      "updated_at": "2026-06-01T21:59:17.353272+00:00"
    }
  },
  "sections": {
    "conflict_and_abstain": [
      "conflict_and_abstain-00011",
      "conflict_and_abstain-00001",
      "conflict_and_abstain-00008",
      "conflict_and_abstain-00010",
      "conflict_and_abstain-00020",
      "conflict_and_abstain-00024",
      "conflict_and_abstain-00025",
      "conflict_and_abstain-00026",
      "conflict_and_abstain-00028",
      "conflict_and_abstain-00032",
      "conflict_and_abstain-00037",
      "conflict_and_abstain-00002",
      "conflict_and_abstain-00005",
      "conflict_and_abstain-00003",
      "conflict_and_abstain-NEW-FATAL-PRIMARY-FIRST",
      "conflict_and_abstain-00063",
      "conflict_and_abstain-NEW01",
      "conflict_and_abstain-00099",
      "conflict_and_abstain-00054",
      "conflict_and_abstain-00004",
      "conflict_and_abstain-NEW02"
    ],
    "expert_output_synthesis": [
      "expert_output_synthesis-00001",
      "expert_output_synthesis-00002",
      "expert_output_synthesis-00003",
      "expert_output_synthesis-00005",
      "expert_output_synthesis-00099",
      "expert_output_synthesis-00016",
      "expert_output_synthesis-00020",
      "expert_output_synthesis-00021",
      "expert_output_synthesis-00022",
      "expert_output_synthesis-00030",
      "expert_output_synthesis-00023",
      "expert_output_synthesis-00050",
      "expert_output_synthesis-000XX",
      "expert_output_synthesis-00035",
      "expert_output_synthesis-NEW01",
      "expert_output_synthesis-00038",
      "expert_output_synthesis-NEW03",
      "expert_output_synthesis-00043",
      "expert_output_synthesis-000NEW01",
      "expert_output_synthesis-00004",
      "expert_output_synthesis-00061",
      "expert_output_synthesis-00060",
      "expert_output_synthesis-NEW05",
      "expert_output_synthesis-00058",
      "expert_output_synthesis-00052",
      "expert_output_synthesis-00051",
      "expert_output_synthesis-00049",
      "expert_output_synthesis-00048"
    ],
    "tool_input_restructuring": [
      "tool_input_restructuring-00002",
      "tool_input_restructuring-00009",
      "tool_input_restructuring-000XX",
      "tool_input_restructuring-00020",
      "tool_input_restructuring-00012",
      "tool_input_restructuring-00021",
      "tool_input_restructuring-00030",
      "tool_input_restructuring-00023",
      "tool_input_restructuring-00029",
      "tool_input_restructuring-00031",
      "tool_input_restructuring-00039",
      "tool_input_restructuring-NEW-FIRST_CALL_FIDELITY",
      "tool_input_restructuring-00062",
      "tool_input_restructuring-00059",
      "tool_input_restructuring-00057",
      "tool_input_restructuring-00055"
    ],
    "expert_routing": [
      "expert_routing-00003",
      "expert_routing-00004",
      "expert_routing-00006",
      "expert_routing-00007",
      "expert_routing-00013",
      "expert_routing-00014",
      "expert_routing-00015",
      "expert_routing-00017",
      "expert_routing-00018",
      "expert_routing-00019",
      "expert_routing-00027",
      "expert_routing-000XX",
      "expert_routing-00033",
      "expert_routing-00034",
      "expert_routing-NEW01",
      "expert_routing-NEW02",
      "expert_routing-00036",
      "expert_routing-NEW03",
      "expert_routing-00040",
      "expert_routing-00041",
      "expert_routing-REFL01",
      "expert_routing-00042",
      "expert_routing-00064",
      "expert_routing-NEW-PRIMARY_MUST",
      "expert_routing-NEW-MANDATORY_PER_PROFILE_EXPERT_CALL",
      "expert_routing-00056",
      "expert_routing-00053"
    ],
    "evidence_grounding": [
      "evidence_grounding-00001",
      "evidence_grounding-000XX",
      "evidence_grounding-00003",
      "evidence_grounding-00010",
      "evidence_grounding-000YY",
      "evidence_grounding-00077",
      "evidence_grounding-00044",
      "evidence_grounding-00050"
    ]
  },
  "next_id": 0
}